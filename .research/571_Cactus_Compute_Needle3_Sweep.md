# Research 571: Cactus Compute / Needle 3 Sweep — 22 Sources Distilled (blog ×14, research papers ×7, Needle 3 product page ×1)

> **Source:** the whole of https://cactuscompute.com/blog, https://cactuscompute.com/research, https://cactuscompute.com/needle (all fetched + distilled via per-content subagents, 2026-09-18). One consolidated note per the sweep; three items earned dedicated notes (568, 569, 570).
> **Date:** 2026-09-18
> **Status:** Done — all 22 items distilled; verdicts per item below; actionable leads consolidated in `.issues/841_cactus_sweep_follow_ups.md` (katgpt-rs) + `riir-train/.issues/561_cactus_training_leads.md`
> **Method:** one subagent per content item (22 total); each ran fetch → pre-flight greps → prior-art check → three-track classification → stack mapping → verdict. Three agents filed their own notes (568, 569/570, issue 839) — 569 was dual-allocated that day and re-pinned (570 keeps the SAN note).
> **Classification:** Public

## Context in one paragraph

Cactus Compute is an on-device inference lab whose entire 2026 output (Needle 1 → 2 → 3) is a working demonstration of nearly the same bet our stack makes: **tiny attention-only models, weight-side quantization (2-bit, never-unpacked), hashed n-gram memory outside the matmul path, grammar-constrained decode, and narrow specialists that beat generalists 10× their size on the narrow task**. Needle 3 = 121M params, 70.8M of them (58%) in engram tables, one weight set where every depth 2–20 is a deployable model, 8–29 MB CQ2 binaries, 400–4k tok/s on a Pi 5. Their engineering blog reads like external validation of Plan 299 (engram), KVarN, lodestar, TernaryDraftModel, and npc_brain — and their papers contain four directly gateable primitives we don't ship yet.

## A. Dedicated notes already filed (3)

| Note | Source | Verdict |
|---|---|---|
| [568](568_Cactus_Cact_Format_Single_File_Quant_Archive.md) | "The .cact Format" (Sandhu, 2026-09-18) — 196-byte header, nameless tensor dir, CQ2 = Hadamard + Lloyd-Max at **2.125 bpw**, kernel never unpacks a weight | **GOAT-plan** (never-unpack CQ-W2A8 GEMV vs Plan 486 slot); archive×NeuronShard×Merkle fusion issue-first |
| [569](569_Hadamard_MLP_Monarch_Kronecker_FFN.md) | "The Hadamard MLP" (Ndubuaku, 2026-09-17) — Monarch A⊗B 32×32 tiles, WHT init, **25.6K params/layer vs 4.7M (180×)**, 22× fewer MACs | **Gain** — issue 839 (`kron_apply` modelless primitive); Monarch-class prior art kills Super-GOAT |
| [570](570_SAN_Attention_Only_FFN_Deletion_Cost.md) | "Simple Attention Networks" (Ndubuaku, 2026-09-17) — FFN deletion costs **0.470 nats iso-depth / 0.263 iso-FLOP / 0.006 iso-param**; loss concentrates on query tokens | **GOAT-plan** gated on an SAN artifact (league cell); query-token diagnostic Gain |

## B. Blog posts distilled here (11)

| # | Source | One-line mechanism | Verdict → routing |
|---|---|---|---|
| 1 | [Designing Tools for Needle 3](https://cactuscompute.com/blog/designing-tools-for-needle) (Kumar) | Small-model competence is **selection, not generation**: every argument a span of the request; required-arg-no-span → call withheld into `suppressed_calls`, never guessed; **~20-point bench move from grounding alone**; constraints compiled into the decode grammar ("invalid value unrepresentable, not discouraged"); >5 tools → hard top-5 retrieval gate ("an unselected tool is unreachable, not merely unlikely") | **PASS** (methodology) — validates Plan 408 (grammar decode fixed format not judgment: parse-fallback 100%→83%, win-rate flat), the healer's oracle-anchor lane, `pool_depth_rescue_guard`. New vocabulary worth keeping: **span-evidence action emission** for NPC actions (info brain = span source; fog-of-war = evidence gate on emission) |
| 2 | [Fine-tuning Needle](https://cactuscompute.com/blog/finetuning-needle) (Mroz) | LoRA r16/α32 on frozen base **through the export numerics (QAT)**, merged at export, sliced to **any depth** — one run, every device. Defaults (bs16, LR 1e-4, 10–30 epochs for hundreds of rows) are **identical to our quest_grammar config**. "`answers: []` off-topic rows ~1 in 8, else the tuned model calls a tool on everything" | **GOAT-plan → riir-train**: QAT-matched merge-at-export + depth-sliced frozen-brain SKUs. Gate: merged-artifact == additive-runtime parity (currently unasserted in our pipelines, Issue 687 H2) + per-depth floors. Modelless keeps: training-surface exclusion contract (confidence head → `None` + warn on tuned weights — we have NO calibration-staleness rule at the freeze/thaw seam); two-pass catalogue→constrained-decode |
| 3 | [Leveraging Needle's Confidence](https://cactuscompute.com/blog/needle-confidence) (Mosoyan) | `confidence = min(calibrated post-hoc head on finished call, decode probability)` — conjunctive AND-gate, "the failure mode is escalation rather than wrong execution"; engine floor 0.1 withholds into `suppressed_calls`; act/confirm/refuse ladder; grounding gates withhold score-independently; **calibration binds to the base model — tuned weights report `None`** (correct Spanish calls measured 0.0) | **Gain**: healer two-signal min-gate on `--fix` writes (Beta-LCB × oracle support → apply/suggest/decline, calibration data already in `.heal/fixseq/`); NPC act/confirm/refuse on ARG Step-8/9; **calibration-staleness rule at snapshot swap** (freeze envelope: invalidate attached calibrators on weight change — never serve a stale plausible score). Not GOAT: selective prediction is Chow-1970 class |
| 4 | [Needle Python Docs](https://cactuscompute.com/blog/needle-python-docs) (Shemet) | Reference surface: grammar-compiled schemas, top-5 tool retrieval, fingerprint-keyed re-embed (schemas AND model), behavior contract (`[]` on no-match, evidence-only args, `suppressed_calls`), deterministic repair layer | **PASS** — every mechanism ships in stronger forms (`ConstraintPruner`, `set_admission`, riir-rag BLAKE3 skip-if-unchanged, `build_stamp`). **One unverified reverse-grep lead**: our `index_code_triples_if_changed` fingerprint may not include **embedder identity** — an embedder swap would keep file hashes matching while embeddings go stale (issue-841 checkbox; verify `retriever.rs` first) |
| 5 | [What Devices Are Supported](https://cactuscompute.com/blog/needle-supported-devices) (Lee) | 13 platform folders, **engine < 1 MB** each carrying no weights, one weights artifact any depth; wasm + **WASI Preview 2 component** (cosign-signed OCI); air-gap install protocol; every response ships `prefill_tps`/`decode_tps`/`peak_ram_mb` | **Gain** (deployment track): derived per-platform "ships" manifest gate (inverse of `wasm32_surface_audit`); WASI-component target only if a host consumer materializes. "Every depth is trained" = vendor claim, riir-train-verify before adopting |
| 6 | [Structured JSON Extraction](https://cactuscompute.com/blog/structured-extraction-with-needle) (Cylich) | Extraction = tool calling with **one tool**; decode grammar compiled from schema before the first token (20-char numeric cap, 64-item/7-identical array cap); **span grounding as a mechanical post-condition** (value must be substring of input; "72 never becomes a 2" — whole-token evidence); required-field-no-span → **whole record withheld**, no partial objects; classification = enum field | **Gain**: suppression-bucket `oracle_edits` (a fix whose replacement isn't derivable from span+corpus is withheld + counted, never emitted); `SchemaGrammar` sampling primitive (katgpt-rs); hard-vocabulary KG emission (decode-time enum on predicate vocabulary, layered on latent-similarity threshold) |
| 7 | [Intelligence Ladders](https://cactuscompute.com/blog/intelligence-ladders) (Ndubuaku) | Nested subnetworks by **bisection**: keep blocks 0 and L−1, repeatedly split widest gap → fixed order `0,19,9,14,4,6,11,16,2,7,12,17,1,3,5,8,10,13,15,18`, subnet d = first d entries. Training: **80% full CE + 20% sampled depth d with (d/2L)·[CE + λ·KL(sg[full]‖p_d)]**, zero wall-clock overhead. Ladder-trained full model **beats the control (1.958 vs 2.037 val loss)**. ⚠ The gotcha worth the whole read: **slicing renumbers blocks — 13 of 14 subnets of a 16-layer slice differed from the parent's; every slice must carry its parent's selection order** | **GOAT-plan → riir-train** (80/20+KL recipe; prior art MatFormer/LayerSkip). Modelless: selection order as a **freeze-envelope datum** (our wire should carry parent order the same way); per-request depth choice unshipped by them (our early-exit slot is empty); per-block KV cache ⇒ depth-proportional memory (league axis) |
| 8 | [Needle 2: The 14 MB Agentic LLM](https://cactuscompute.com/blog/needle-2) (Ndubuaku) | 45M SAN: 27L×512, 4 residual lanes, WHT+diag FFN, engram = 8M params read by gather (70 vs 87 MFLOPs/token matched-params), **CQ2 QAT from pretraining incl. KV cache**, 256-token sliding window + **system prompt/tool decls pinned as permanent KV sinks** (28 MB deterministic ceiling), grammar skips **≤98% of vocab projection** rows, thread-pool spin "alone nearly doubled decode". Eval honesty: shipped-binary production-config numbers, stated asymmetries both ways; Mobile-Actions 63.7 strict EM but **name accuracy 98.3 best-in-table** (losses = argument values, not function choice) | **Gain**: 3 GOAT-plan candidates — (a) KV permanent sinks + bounded window (league + serving; nothing ships — grep clean), (b) grammar-forced **projection-row skip** in spec decode (`lodestar` knows the legal set but we still score the full vocab), (c) Hadamard-MLP arch slot. **Super-GOAT-shaped fusion candidate (novelty TBD, issue-first)**: engram-delta as the modelless fine-tune — write product tool-vocabulary into `StagingEngramTable`, commit, hot-swap, chain-commit; GOAT gate vs LoRA fine-tune on accuracy/latency/training-cost=0. riir-train seed: KV-cache QAT (our LR-QAT covers weights only) |
| 9 | [Tool Environments for Needle 2](https://cactuscompute.com/blog/needle-environment-best-practices) (Shemet) | Execution-side doctrine from 6,120 runs: **"confidence did not reliably separate correct and incorrect calls — do not use confidence as an authorization policy"**; tool order swung exact accuracy **up to 12 pp** (freeze it); **refusal ≠ truncation** (empty list after token-budget error is not a clean refusal); dynamic enums from live state (pass the 5 open case IDs); state lives outside the model; stage-wise failure attribution | **Gain** (doctrine + 3 small guards): healer verify-path **refusal-vs-truncation sentinel** (kill vs pass must be distinguishable in `fix_verify` — flagged unverified, audit first); order-pinning for riir-agents shared-context composition (our own Issue 121: `all_rules` canonical-order fix **+55.6pp** — same finding shape); observation-narrowed valid-target table for NPC action selection |
| 10 | [Needle: Distilled Gemini Tool Calling into 26M](https://cactuscompute.com/blog/needle) (Ndubuaku, 2026-05-12) | Needle 1: **data distillation** (Gemini synthesizes the post-train corpus — 2B tokens/45 min on 16 TPU v6e), not logit-KD; attention+gating **zero FFN**; "tool calling is retrieval-and-assembly, not reasoning"; 6000/1200 tok/s prefill/decode on consumer devices; beats 270–350M models single-shot | **Gain → riir-train**: a **data-distillation lane is missing** (`distill_shared.rs` has SDAR/DASD logit-KD only). Fusion: distilled ternary npc_brain — mine/synthesize (obs→action) pairs, train tiny student, quantize into the **existing** `TernaryWeights` vessel + freeze wire; GOAT gate = scenario win-rate vs hand-designed `build_reference_brain()` |
| 11 | [TurboQuant-H](https://cactuscompute.com/blog/turboquant-h) (Mosoyan, Ndubuaku, 2026-04-21) | Offline variant of TurboQuant ([Research 020](020_TurboQuant_Online_Vector_Quantization.md)): normalized **Hadamard** (self-inverse, O(N log N)) + **per-position** Lloyd-Max codebooks trained on actual weights (0.125 bpw overhead) + **QJL dropped at 2-bit**; Gemma 4 E2B PLI embeddings (60.6% of weights) 2,496→624 MB (4×), total −40%, **+0.06 PPL**; <2 µs/lookup on NEON; "per-position was consistently better" than joint codebook | **GOAT-plan**: riir-rag embedding-index lane — 2-bit via shipped `group_lut_at` 4-entry LUTs; **gate = recall@k vs f32 index, NOT PPL** (our own evidence: values cos_sim 0.94 at 2-bit — unproven axis); open question: quantize-then-wedge vs wedge-then-quantize vs Clifford-wedge KNN. Bonus A/B: per-position vs joint codebook on the demoted `TurboQuantKVCache` |

## C. Research papers distilled here (7)

| # | Paper | One-line result | Verdict → routing |
|---|---|---|---|
| 12 | **A Controlled Study of Attention-Only Transformers** — Ndubuaku et al., [arXiv:2607.18363](https://arxiv.org/abs/2607.18363) | Full-paper matrix behind 570: iso-param gap **0.046@5B → 0.019@30B → 0.0055@105B** (separately trained+tuned budgets); gap **plateaus ≈0.02 nats from 16M–57M, does not grow with scale**; SAN *wins* at tiny (−0.045, FFN partner hits depth floor); **QK-norm removal = the study's only divergence (loss 8.28)**; sandwich norm −0.0092 the only variant win; **optimal Muon LR differs 2× between arms** — a shared rate silently biases any comparison; **Q/K spectrally crystallize in the first quarter of training and do not move**; FFN removal relocates rank accumulation to W_o; 8 pre-registered predictions, 2 falsified (the central finding emerged from a falsification); region decomposition: query deficit is the ONLY sign-invariant; repetition (18 epochs) costs ≤0.010 nats, no arch×repetition interaction; per-arm LR sweep table + noise floor 0.0015 + determinism check (cold-optimizer restore reproduced final loss exactly) | **GOAT-plan → riir-train**: SAN pretraining recipe plan (per-arm Muon LR fairness, QK-norm mandatory, sandwich norm, zero-centered gains + kernel-only decay, 20L depth optimum, boundary-extension sweep protocol). Modelless: **crystallization meter** (stable-rank trajectory as a modelless freeze criterion for MerkleFrozenEnvelope — signal-diff vs `spectral_flatness.rs` first); SAN league cell gated on converting a shipped checkpoint. ⛔ NOT a Bonsai FFN-deletion license: 27B ≫ 87M, iso-depth 0.470 nats is what in-place deletion would cost |
| 13 | **Parameter-Efficient Transformer Embeddings via Functional Factorization (PETE)** — Ndubuaku, ICML 2026 ([arXiv:2505.02266](https://arxiv.org/abs/2505.02266)) | Replace the V×d embedding **table** with a **function of the token ID**: `x = 2p/(V−1)−1` → fixed Fourier basis (sin/cos families — RoPE math applied to IDs) → `E(p) = MLP(T(p)) + T(p)`. Matched-quality at 8.9M vs 24.3M baseline (77.40 vs 77.54 Spearman, 2.7× fewer params); **training 1.3× faster** (no V×d optimizer state); beats tiny-model leaderboard STS-B (81.7/81.9 at 3.6M vs BERT-Small 29M 78.8/77.0). Failure mode named: **near-colliding embeddings** (BPE adjacency ≠ semantic adjacency, Δx = 2/(V−1)) | **GOAT-plan**: `functional_embed` flag in katgpt-rs — `T(p)` kernel (new consumer of the `RopeFreqs` shape) + **closed-form table distillation**: regress shipped `wte` rows onto Fourier features via KARC ALS — **zero gradient descent**, our deterministically-constructed-adapters pattern exactly. Gates: ≥0.98 mean cosine on Bonsai table + riir-rag recall + decode. Free falsification first: permute-tokens ablation on our BPE tokenizer. Caveats: unembedding untouched; ICML-version generalized bases unverified (OpenReview CAPTCHA) |
| 14 | **Depth Over Specialization in Small Multimodal Transformers** — Mroz, Ndubuaku, ICLR 2026 WS ([OpenReview rMrUJyrKHk](https://openreview.net/forum?id=rMrUJyrKHk)) | At **matched transformer params** (4.2M vs 4.2M; unit = 2 layers), one **shared trunk beats per-modality towers** on contrastive text/image/audio retrieval; merging towers ⇒ **~2× transformer-param cut at parity** (2.1M shared ≈ 4.2M separate); third modality **lifts the weak pair, degrades strong pairs** at fixed capacity. Ships a **gradient-cosine interference probe** (per-pair loss-gradient similarity on shared trunk) as the triage tool. PDF CAPTCHA-walled; verified via abstract + authors' repo (92,987 samples, d256/h8, InfoNCE, 3 seeds) | **Gain**: capacity-allocation default for any small game-facing multimodal encoder — **spend new capacity on trunk depth, not modality experts** (validates npc_brain's single shared trunk; the freeze/thaw wire makes a depth-vs-tower A/B cheap: two frozen brains, same action-eval suite). Gradient-cosine probe generalizes to any multi-objective shared-trunk training in riir-train (multi-task distillation, GRPO+aux): persistent negative cosine = split heads or grow capacity. Upgrade to GOAT-plan only if the vision+game-state lane opens |
| 15 | **Calibration-Aware Activation Sparsity (CLAWS)** — Cylich, Mosoyan, Ndubuaku, ICML 2026 WS ([OpenReview kdZbHVOjd6](https://openreview.net/forum?id=kdZbHVOjd6)) | Dynamic top-K FFN sparsity on instruction-tuned models by scoring `gate_scoreᵢ(token) × cᵢ` where **cᵢ is a static Fisher-style per-neuron constant** estimated offline; row-sparse mask aligns with quantized K-blocked layouts (skip whole Q4_K superblocks dequant-free). **Gemma-4-E2B-IT at 50% density: dense-level Avg5, +6.8pp MMLU over matched-recipe CATS+LoRA (49.6 vs 42.8), 1.24× ARM MLP wall-clock**. The transferable law: **a static importance modifier only works when the base score's dynamic range is compressed** (GELU-gated scores: yes; rotated-input scores: no) | **GOAT-plan**: activation-sparse decode cell llama.cpp can't follow — per-neuron `cᵢ` LUT (offline calibration) × GeGLU gates → `argtopk_with_scratch` mask (ships in katgpt-attn; ⚠ re-run x86_64 matrix — Bench 806 found an argtopk defect class) → Q4_K GEMV skips masked superblocks; composes with ternary (shrink rows that run × remove rows entirely). Full PDF CAPTCHA-walled: Fisher formula, corpus size, per-layer-vs-global scope unverified — pull before the plan lands. The dynamic-range law audits our own static-prior×runtime-score sites (set_rerank, pick_domain) |
| 16 | **A Blazing Fast LM-Head Replacement (HiDRA)** — Kumar, Ndubuaku, ICLR 2026 TTU WS ([OpenReview I02VQSYShw](https://openreview.net/forum?id=I02VQSYShw)) | Binary decision tree over tokens: L ≈ log₂V hyperplane decisions then full head only over the leaf set. **Tree construction = Fisher/LDA median split on the embedding matrix (closed-form, modelless init)** — "token embeddings do not form clean clusters, making k-means less suitable"; per-node separators fine-tuned (BCE) on teacher-forced hidden states; sigmoid-product routing (full softmax never computed — house rule resonance). **8.04× output stage / 1.24× e2e** at −4.2pp top-1 (l=4,k=4); OOD retention 95.8%; depth+beam = inference-time dial, zero retrain | **GOAT-plan — re-opens our Bench 688 PERMANENT NEGATIVE**: clustered-LM-head failed on real checkpoints (99.95% admissible-active, Gemma 2 2B tied wte) and HiDRA's anisotropy argument predicts that failure **verbatim**. HiDRA-v2: LDA-init tree (modelless arm FIRST per mandate) → per-node BCE via riir-train only if the modelless arm stalls; reuse Issue-666 `ClusterLayout` packed rows (contiguous leaf spans = the layout HiDRA wants); gate on the same 123-probe harness. ⚠ Uncosted: router storage ≈ V internal nodes × d — same size as the head; must quantize (sign-only hyperplanes). League scope: sub-1B/CPU and MTP-verify (N×vocab compounding), not Bonsai tg128 headline |
| 17 | **Just Enough Learning (JEL): GRPO-Guided Controllers for Hyperparameter Sweeps** — Lee, Ndubuaku, ICLR 2026 RSI WS ([OpenReview kKWSQsYgpa](https://openreview.net/forum?id=kKWSQsYgpa)) | Tiny GRPO-trained policy net emits multiplicative LR/WD corrections at fixed intervals atop schedule-free optimizers: **+2.5% validation at equal compute; controller cost ≈ 5.6 training runs ONCE** vs 6–8 runs per task forever; within 8% of hand-tuned. The named contribution: **removes length + per-group variance normalizations from GRPO** (the Dr. GRPO bias correction) — confirmed in a second domain. PDF Turnstile-walled; abstract+TL;DR verbatim, GRPO internals labeled inferred | **Gain → riir-train, one cheap item first**: **audit `loss_grpo.rs`** — if our advantage divides by group σ (or normalizes by response length), A/B the unbiased form `Aᵢ = rᵢ − μ_group` on one alignment run (two independent published confirmations, zero new infra). The controller itself right-sizes as a **modelless quantized bandit** (state bucket → {×0.5..×2.0} LR corrections, Beta-LCB with mean-centered rewards, conditional GOAT gate: beats fixed schedule at fewer runs) — not a trained policy net; riir-dao/self_evolve need nothing (relative no-value-function selection already ships) |
| 18 | **Token-Aware Chunked Encoding (TACE)** — Sandhu et al., ICLR 2026 NFAM WS ([OpenReview yUIOXlBYtP](https://openreview.net/forum?id=yUIOXlBYtP)) | Post-training conversion of a RoPE **ASR encoder** to fixed-shape chunked execution: C=2s chunks + L=0.5s left overlap, token-unit-exact deterministic stitching + a 2-layer learned stitcher, LoRA state-regression + decoder logit-KL vs the frozen full-context teacher; **1.33× average encoder speedup (2.05× on 25–30 s), WER within 5 points**, native streaming contract | **PASS** (domain-mismatched: bidirectional ASR encoder — our causal text prefill needs no seam repair; no tokenizer/prefill-league impact). The conversion recipe (state-regression + logit-KL under a frozen teacher) is the shape the HLA lane would use if ever formalized; relevant only if an on-device ASR/voice lane opens |

## D. The Needle 3 product page (1)

**[Needle 3 — Automation Foundation Model For Tiny Devices](https://cactuscompute.com/needle)** (2026-09-18). The full-system spec the blogs split apart: 20×768, 121M params, **70.8M in engram tables (58%)**; SentencePiece BPE 8,192; **mHC 4-lane residual** — read `u = Σₙ σ(a φₙᵀ x̂ + b) Xₙ`, write `X′ = P X + 2σ(h) ⊗ y` with **P = Sinkhorn(A)** (doubly-stochastic lane write) + sandwich ZCRMSNorm; engram fusion `x ← x + σ(⟨x̂,k̂⟩/√d)·v` at sites 3/7/11/15/19, 18,432 slots, by gather; GQA 12q:2kv (48 qk / 64 v — asymmetric head dims) + 3-tap causal conv on q + RoPE + QK-norm; window 1024, **global at 4/9/14/19**, `o = σ(g)⊙softmax(...)`; Monarch Hadamard FFN; byte-level grammar; ladder 2..20 (0,19 kept); **MTP t+2 aux head (training only)**; 360B-token proprietary structured dataset. Benchmarks: Mobile Actions 961 / DroidCall 200 / BFCL v4 3,641 (AST match, no-call-on-irrelevant) / DSTC8 1,813 / SNIPS gold+7-way 700 each; **Figure 2 caveat: Needle scored through the shipped CQ2 binary, baselines at f16 under vLLM** (carries both ways — quote with the claim).

**Verdict: Gain** — a product spec, not a paper: every component has published prior art, but the **composition is a validated, priced bill of materials for our tiny-device lane** assembled from parts we ship in pieces:
- **Engram fusion ≈ our Plan 299/360 substrate, near-identical gate math** (`sigmoid_fuse_into`, τ=√d already matches, trigram hashing) — and their gate is relevance-only, i.e. **exactly the gap Issue 656 fixed on our side**; our fix is strictly better. New datum: engram tables as 58% of params (memory floor is tables, not layers — the devices post's Pi numbers confirm: "even the 2-layer model keeps most of its memory").
- **mHC Sinkhorn lane write is a new usage of shipped `sinkhorn_plan`** (3 modules ship it): balanced per-lane contribution = the kind of bridge function our sync rules want where multi-lane latent state approaches a sync boundary. riir-train xHC (Issue 383, arXiv:2607.14530) is the training-side cousin.
- 3-tap conv / GQA+RoPE+QK-norm / MTP / confidence calibration: **ship** (ShortConv1D k=3-vs-4 config delta; asentmax Qwen3 path; MTP aux-as-deep-supervision is a riir-train note).
- **New**: windowed+global σ-gated attention kernel, Monarch FFN (→569/839), depth ladder (→ladders row), byte-grammar decode, triggers-restriction.

## E. Cross-cutting findings (what the sweep says about our stack)

1. **External validation, unusually direct.** Independent lab, same year, shipped product: attention-only tiny models + hashed n-gram memory + never-unpacked 2-bit weights + grammar-constrained decode + narrow-specialist-beats-generalist + confidence-gated refusal. Our modelless bets (Plan 299 engram, ternary/Plasma, lodestar, npc_brain, refuse-never-wrong) are now corroborated by someone else's benchmark table — including the SAME failure modes we fixed (their relevance-only engram gate = our Issue 656; their uncosted router storage = our Bench 688 lesson; their "confidence is not a policy" = our evidence-tier doctrine).
2. **Four directly gateable primitives we lack**: CQ-W2A8 never-unpack GEMV (568), activation-sparse MLP decode (15), HiDRA-v2 LM head (16), functional embeddings (13). All GOAT-tier, all with concrete gates.
3. **Two training recipes with measured wins**: the depth ladder (80/20+KL, +free-regularizer effect 1.958 vs 2.037) and QAT-matched merge-at-export; both fit riir-train's existing pipeline shapes (Path 0.5, not redirects).
4. **Two boundary rules to adopt cheaply**: calibration-staleness on snapshot swap (freeze envelope invalidates attached calibrators — we serve stale plausible scores today in principle) and refusal-vs-truncation discrimination in `fix_verify` (audit first).
5. **Vendor-claim hygiene**: every Needle accuracy number is self-reported through their own engine; Figure 2 compares CQ2-shipped vs f16-baselines; "every depth is trained", the DeepSeek-V4-Flash pass, and the 4L ladder numbers must not enter a plan without our own fixtures (the x86_64-matrix lesson: a benchmark IS its fixture).

## PASS-Redirects (so future greps hit this note)

- **PASS-Redirects (synthesis):** Sandhu et al. 2026, "The .cact Format" (cactuscompute.com/blog/cact-format) → [568](568_Cactus_Cact_Format_Single_File_Quant_Archive.md).
- **PASS-Redirects (synthesis):** Ndubuaku 2026, "The Hadamard MLP" (cactuscompute.com/blog/hadamard-mlp) → [569](569_Hadamard_MLP_Monarch_Kronecker_FFN.md) + Issue 839.
- **PASS-Redirects (synthesis):** Ndubuaku 2026, "Simple Attention Networks" (cactuscompute.com/blog/simple-attention-networks) + "A Controlled Study of Attention-Only Transformers" [arXiv:2607.18363] → [570](570_SAN_Attention_Only_FFN_Deletion_Cost.md) + §C-12.
- **PASS-Redirects (synthesis):** Kumar/Satyajit 2026, "How to Design Tools for Needle 3" (cactuscompute.com/blog/designing-tools-for-needle) — span-grounding/suppression vocabulary; validates Plan 408 + Research 183 spine + Research 026 masking; no shipped gap.
- **PASS-Redirects (synthesis):** Shemet 2026, "Needle Python Docs" (cactuscompute.com/blog/needle-python-docs) — all mechanisms ship in stronger forms; one reverse-grep lead → issue 841.
- **PASS-Redirects (synthesis):** Mroz/Ndubuaku 2026, "Token-Aware Chunked Encoding" (ICLR 2026 NFAM WS, OpenReview yUIOXlBYtP) — **domain-mismatched PASS**: ASR speech-encoder chunked execution (1.33× encoder, WER within 5pts, C=2s/L=0.5s, LoRA+state-regression+logit-KL vs frozen teacher); our causal text prefill needs no seam repair; the conversion recipe is the HLA lane's shape if ever formalized. No prefill-league or tokenizer impact.

## Issue leads (filed: `.issues/841_cactus_sweep_follow_ups.md` katgpt-rs; `riir-train/.issues/561_cactus_training_leads.md`)

katgpt-rs / league / healer: CQ-W2A8 GEMV gate · .kpt archive fusion (novelty-TBD issue-first) · CLAWS sparse-GEMV cell · HiDRA-v2 on Bench 688 · TurboQuant-H riir-rag recall@k lane · KV permanent sinks + bounded window · grammar-forced vocab-projection skip · functional_embed (PETE-distill) · embedder-identity fingerprint check · engram-delta-as-fine-tune (novelty-TBD) · calibration-staleness rule · fix_verify refusal-vs-truncation audit · SAN league cell (gated on artifact) · crystallization freeze criterion (signal-diff vs spectral_flatness first).
riir-train: depth-ladder recipe (80/20+KL) · QAT-matched merge-at-export + sliced SKUs · GRPO bias audit of loss_grpo.rs · data-distillation lane (Needle 1) · KV-cache QAT seed · distilled ternary npc_brain · SAN pretraining recipe note · gradient-cosine interference probe.

> **Noise note:** this session's per-content subagents wrote three files concurrently from a gitignored highwater — Research 569 was claimed twice (Hadamard MLP + SAN) and re-pinned same-day (SAN → 570). The `.highwater`-is-local-ledger hazard, live instance #1.
