# Research 560: NAP Invariants + Meld — Bounded Non-Associative Composition for Latent State

> **Source:** [Formal Properties of Language as Constraints on Neural Dynamics](https://arxiv.org/abs/2609.14384) — Elliot Murphy. arXiv:2609.14384v1 [cs.CL], 13 Sep 2026. 11,253 KB (PDF-only; no HTML render).
> **Date:** 2026-09-15
> **Status:** Active — **Gain (GOAT-tier)** verdict; Super-GOAT gate blocked on Q3 quality PoC → **Issue 801 filed** (NAP audit + `meld` primitive + riir-poc PoC). **PDF read 2026-09-15** (`curl` + `pdftotext` after jina/html failures): equations transcribed below; the initial abstract-level reconstruction was WRONG in the mixture term (Hadamard→L2 guessed; actual = entropy-optimized soft-min mixture through tied weights) — corrected throughout, T1 of Issue 801 discharged in-note.
> **Related Research:** 527 (TPR binding algebra — the closest shipped cousin: substructure access via explicit roles, unbounded bilinear), 299 (Clifford wedge — overlap/disagreement as pairwise signal), 495 (spectral pencil), 244 (adaptive mod), riir-neuron-db 309 (slice TCA covariability consolidation), 302 (HOPE capacity), 305 (plasticity lifecycle); riir-clippy draft `jury_disagreement.rs` / `withheld_pair_ood.rs` (disagreement + OOD eval axes)
> **Related Plans:** none yet — consumer plans gated on Issue 801 PoC (no-default-consumer rule, 527-F2 precedent)
> **Classification:** Public (the distilled primitive is generic math; no game/chain/shard semantics)

---

## TL;DR

Murphy proposes the **Neural Admissibility Program (NAP)**: algebraic properties of language — **non-associative hierarchical grouping, commutativity, recursive closure, access to substructures, structured workspace transitions** — specify *invariants* that any mechanism implementing structure-building must preserve, and predictive (decoding) success alone cannot adjudicate between candidate mechanisms. He corrects the Marcolli–Berwick entropy-optimized binding gate (arXiv:2305.18278 / 2507.13501 — the Rényi-entropy semiring), showing it preserves grouping only in a narrow commitment band, and proposes **Meld**: two constituent populations **converge through shared synapses, integrate sublinearly, and saturate** — claimed the closest neurally plausible composition law to syntactic Merge, preserving every NAP invariant, recovering hierarchy at every tested depth/temperature, with two testable predictions: **effective population dimensionality separates alternative bracketings**, and **the composite depends on constituent disagreement**.

**Distilled for katgpt-rs (modelless, inference-time):** the payload is algebra + closed-form dynamics + an eval methodology — no training anywhere (no optimizer/loss/RL; simulations are ODE-style population dynamics ⇒ adversarial panel not triggered). The actual law (paper §4.9.1, verified): **`Meld(u,v) = tanh(κ·W·[λ⋆u + (1−λ⋆)v])`**, κ=2, where **λ⋆ is the offset-corrected Rényi gate's argmin** — a *pointwise soft minimum* (`λ⋆ = argmin_λ [λu + (1−λ)v + (1/β)·log(λ²+(1−λ)²)]`, S₂(λ) = −log[λ²+(1−λ)²]) that rewards daughter agreement and penalizes disagreement — passed through **tied orthogonal weights W** (the "shared synapses"; tying IS the commutativity requirement — untied weights leak daughter order) and a **saturating transfer**. The paper's own ablation hierarchy is the load-bearing finding for us: the soft-min (disagreement-sensitivity) and the tied non-permutation W (lifts bracketing separation from 4th to 2nd order) are *forced by the algebra*; **the saturation contributes ONLY boundedness** ("any bounded nonlinearity would be appropriate"; divisive normalization performs equivalently/slightly better at depth; the *unsaturated* linear tied mixture "law 8" matches Meld on every axis: 1.000/1.000/0.998/0.964/0.899 across d=1–5).

Two extractions: (1) **NAP-admissibility audit** — a 5-invariant battery for composition mechanisms, which when pointed at OUR stack proves a real gap: *every shipped composer is bracketing-blind* (mass-weighted mean, EMA lerp, gated sums, linear blends are associative/commutative ⇒ grouping and order information is provably destroyed; TPR preserves structure only via explicit role bookkeeping, not via its law). (2) **`meld`** — a bounded, commutative, non-associative, disagreement-coding composition primitive (the law above). Bounded magnitude under arbitrary composition depth (fixed-size Pod f32 safety), grouping retention, disagreement trace (λ⋆ → min-like as β·|u−v| grows: the composite stops averaging where constituents contradict).

**Verdict: Gain** (GOAT-tier fusion, modelless-validable). Not Super-GOAT *yet*: Q3 (product selling point) is quality-contingent — "NPC memories preserve how experiences grouped" and "consolidated shards stay decomposable" are finishable sentences but unproven; §3.6 requires a PoC before quality claims. Issue 801 filed (same shape as 527's Issue 707).

---

## 1. Paper Core Findings (PDF-verified 2026-09-15)

1. **The Neural Admissibility Criterion (NAC).** A theory F with objects S, operations M_k, invariants I_j; an implementation is an encoding N: S → X plus physiological ops G_k, admissible only if the relevant diagrams commute up to stated equivalence and the **invariants I_j remain recoverable under N**. "Mere correlation with a variable defined by F is not sufficient to establish implementation" — the methodology mirror of our §3.6 (architectural ≠ quality parity) and withheld-pair OOD discipline.
2. **The three-fold failure of linear mixtures** (§4.9): (a) nothing fixes the mixture weights Λ (a family of maps, not a map); (b) with Λ free the output leaves the type (closure fails at the first step); (c) the λ-side vs (1−λ)-side is a *planar* structure — it orders the daughters (non-commutativity smuggled in). Fixing λ=½ (mean-pooling) discharges all three but is **content-independent** — "part company only on contrasts that a fixed mixture cannot resolve."
3. **The extensivity correspondence** (thermodynamic-semiring axiom correspondence, after Marcolli–Thorngren 2014): ⊕_{S,β} commutative ⟺ S symmetric; associative ⟺ **S extensive**; all three axioms together ⟺ S is Shannon. **Non-associativity IS the failure of extensivity** — the entropy of a grouped mixture not recoverable from its parts. "A candidate neural gate that is symmetric in its daughters and non-extensive in its scoring is thereby a candidate Merge; one that is extensive is not, whatever else it does." Mean/sum/EMA composers are Shannon-extensive ⇒ **provably inadmissible** as structure-preserving composers.
4. **Rényi order selection**: α=2 is the largest order at which the gate's variational problem is convex in λ; above α=2 the minimizer is *discontinuous* (jumps of 0.05–0.39 as daughter difference crosses a threshold) — disqualifying for bounded noisy dynamics.
5. **The pointwise-class theorem**: every pointwise, commutative, translation-equivariant gate has the form `g(x,y) = (x+y)/2 + h(|x−y|)`; a balanced depth-d tree evaluates to `A + d·h(0)`, bounded in depth exactly when idempotent; the balanced four-leaf pair is separated only at **fourth order** (ε⁴) by this whole class. Mean-pooling: leaf mixture `a_ℓ = 2^−depth(ℓ)` fixed by tree shape alone ⇒ **any two bracketings with the same leaf-depth multiset are provably indistinguishable** (identical S₂, identical D_eff).
6. **Meld** (§4.9.1): `tanh(κW[λ⋆u + (1−λ⋆)v])`, κ=2 — soft-min mixture λ⋆ (rewards agreement, penalizes disagreement) + tied orthogonal W (commutativity; lifts four-leaf separation to **second** order via the `(A−D)∘(B−C)` cross term, vanishing only for permutation W) + saturating transfer (**boundedness only** — any bounded nonlinearity; divisive normalization equivalent/slightly better at depth; unsaturated "law 8" `W[λ⋆u+(1−λ⋆)v]` matches on every axis). Depth ladder: bracketing 1.000/1.000/0.998/0.970/0.922 (d=1–5); law 8: 1.000/1.000/0.998/0.964/0.899; removing W: 0.843/0.754/0.705/0.677/0.684 (W is load-bearing). Inadmissible ordered role-filler law: 0.972 at d=5; recurrent law (untied weights): 0.799. **No temperature window**: ≥0.99 for β ∈ [0.03, 30] (β→0 degrades to tied-weight unit, β→∞ to saturated tied minimum — both carry structure).
7. **Effective dimensionality readout** (§9.2): the collision entropy IS the log participation ratio — `S₂(A) = −log Σ_ℓ a_ℓ² = log D_eff(A)`. Honest caveat inside the paper: the *deterministic* superposition Ψ(T)=Σ a_ℓΨ(ℓ) has power a_ℓ², so PR over the squared-amplitude distribution gives `exp(3S₄ − 2S₂)`, NOT exp(S₂) — the clean identification needs variance linear in mixture weight (stochastic spiking in cortex; a design constraint for any of our deterministic readouts).
8. **Division of labor** (§6.7): expander-Hopfield store = the memory/workspace; threshold-linear (CTLN) sequencer = the derivation scheduling; the composition gate = the binding step. Structural mapping to our substrate: ShardIndex (BLAKE3-keyed content-addressable zone→shard) ≈ the workspace store; phase_gate/mape_k ≈ the sequencer; `meld` ≈ the binding gate. Plus §5's phase-address code + order-free hash-key — a content-addressable memory via hash keys is literally ShardIndex's access pattern.

---

## 2. Distillation — Path 0 inventory (coverage × extraction)

| # | Paper component | Coverage in stack | Modelless extraction | Disposition |
|---|---|---|---|---|
| 1 | NAP 5-invariant admissibility battery | **none as a discipline** — no composition primitive is tested for invariant preservation; components ship piecemeal (tpr `unbind` = substructure; Pod ops = recursive closure) | Test battery: grouping retention / commutativity / recursive closure / substructure access / workspace-transition structure, per composer | Issue 801 T2 |
| 2 | Meld composition law (PDF-verified) | **none as a conjunction** — census (subagent, 2026-09-15): `CommittedFieldBlend` is a σ-gated *linear* sum with content-independent gates (π from `summary·dir_k` per-archetype — the mean-pooling class of finding 2/5); Raven `sleep()` is mass-weighted mean (**exactly** fixed-λ mean-pooling — provably bracketing-blind on same-depth-multiset contrasts); `tpr_bind` is bilinear (substructure via roles, unbounded); Clifford wedge is a pairwise signal (never fuses); `frozen_attractor` heal is a lerp | `meld(u,v) = sat(κ·W·[λ⋆u + (1−λ⋆)v])`: pointwise soft-min λ⋆ = argmin_λ[λu+(1−λ)v+(1/β)log(λ²+(1−λ)²)] (closed form = quadratic in λ; β→0 mean, β→∞ min) + tied orthogonal non-permutation W + tanh or divisive normalization. Commutative exactly (swap-symmetric argmin), non-associative via nested saturation+mixture, type-preserving D→D, bounded by construction | Issue 801 T3 |
| 3 | Bracketing-blindness of mean/EMA/lerp/sum composers | **our finding from applying NAP to our stack** — nobody has audited consolidation for algebraic invariant preservation; the paper now SUPPLIES the theorems (extensivity correspondence §1.3; pointwise-class + depth-multiset §1.5) | Audit rows: mean/EMA/lerp are Shannon-extensive ⇒ inadmissible; fixed-mixture composers provably identical on same-leaf-depth-multiset bracketings; catastrophic averaging = the recursive-closure failure mode | Issue 801 T2 |
| 4 | Effective dimensionality separates bracketings | partial — `participation_ratio` ships (`subspace_phase_gate.rs`, chunk-4 SIMD, zero-alloc); nobody applies it to composition bracketing | `S₂(A) = log D_eff` — PR is EXACTLY the predicted readout (§1.7); deterministic-superposition caveat: PR over squared amplitudes reads exp(3S₄−2S₂), still separating but not the clean exp(S₂) | Issue 801 T4 |
| 5 | Constituent-disagreement dependence | partial — `jury_disagreement.rs` (pool spread, selection-side), wedge (structural independence), hebbian `verify_margin` γ_min (crosstalk margin) — all *selection* signals; no composer *encodes* disagreement into the composite | Disagreement trace IN the composite (low shared-mass where constituents contradict) → surprise signal feeding cgsp curiosity / consolidation gating | Issue 801 T3/T4 |
| 6 | Decoding-accuracy ≠ admissibility | shipped discipline (§3.6; withheld-pair OOD; T6A counterfactual refutation) | Reinforces + extends: add invariant-preservation axes to binding eval beyond top-1 | folded into 801 T2/T4 |
| 7 | MB entropy-gate commitment band ⚠ | none (we ship no entropy-optimized binding) | context only — informs the Meld-vs-alternative comparison | recorded, no action |
| 8 | Phase-coupled sealing / heteroclinic workspace scheduling | structurally adjacent — `phase_gate.rs` (consolidation phase gate), `frozen_attractor`, cgsp collapse bridge; Merkle seal = the commitment analog | grouping-recorded at seal time: composite emits KG provenance triples `(constituent, grouped_with, constituent)` — vibe.rs consumption | fusion F6, consumer-gated |

**Path 0 verdict: MODELLESS-VALIDABLE.** Every actionable row is closed-form algebra, an eval protocol, or an audit. The paper carries no training recipe — the three-track adversarial panel is not triggered (no optimizer/loss/RL content; simulations are dynamical-systems, not GD).

---

## 3. Fusion

**F1 (primary): Meld × Raven/δ-Mem × freeze/thaw — structure-preserving consolidation.** `sleep()`'s mass-weighted mean is associative ⇒ provably destroys batch grouping and converges to mush at depth; `consolidate()`'s EMA α=0.3 is a lerp (same class). Meld-composed shards: bounded magnitude (Pod f32 safety under recursive closure), grouping retention (sleep-cycle batch structure = the bracketing), disagreement trace (innovation encoded, not averaged away), lineage BLAKE3-frozen per cycle. None of the three alone gives bounded, decomposable, structure-retaining consolidation. → gated on 801 PoC.

**F2: Meld × CommittedFieldBlend.** The blend's gates π_k come from `summary·dir_k` per-archetype independently — content-independent mixing, which by the paper's pointwise-class theorem (§1.5) lands it in the mean-pooling class: same-depth-multiset grouping contrasts are provably invisible to it. Meld supplies exactly the two load-bearing missing pieces: the content-dependent soft-min mixture (disagreement-sensitivity — archetypes never see each other today) and the tied shared transform (commutativity with second-order pairing separation). Its σ saturation is the part the paper says is least load-bearing — keep it for Pod boundedness only.

**F3: NAP-audit × tpr — roles-vs-law, two admissible routes.** TPR sum is associative; it preserves grouping only because *roles encode tree positions* (structure via explicit bookkeeping). Meld preserves grouping via the law itself (structure implicit in the composition dynamics). For fixed-size Pod latent states with no role slots, the law route is the only option — and the audit battery scores both routes on the same axes. Also: tpr's T6A withheld-pair harness gains an invariant-preservation axis (finding 6).

**F4 (consumer, riir-clippy): Meld disagreement × jury_disagreement × multi-span composition.** `compose_multi_strided` fixes spans with implicit coupling (the closure-birth mechanism: coupled spans birth lints together); a Meld-composed fix pool would *encode* span disagreement (jury spread) instead of majority-voting it away, and substructure access = per-rule edit attribution (explainability). New eval axis on the withheld-pair OOD bench. → recorded; consumer issue after 801.

**F5 (game, riir-ai): structure-aware NPC memory.** `(ambush→flee) ≠ (flee→ambush)` as *grouped* memories; disagreement trace → curiosity trigger (cgsp); PR of belief composites separates routine bracketings without explicit parsing. **Selling point contingent on PoC** — this is the Q3 blocker.

**F6: Meld × KG-triple emission.** At seal/commit time the composite emits grouped-with provenance triples (vibe.rs) — substructure access made durable.

---

## 4. Novelty gate (§1.5)

**Pinned claim (pre-search):** *"Meld — a saturating, sublinear, shared-component composition law — as the first bounded, non-associative, disagreement-coding composition primitive for latent shard/belief consolidation, plus the NAP 5-invariant battery as an admissibility audit for composition mechanisms; distinguished from CommittedFieldBlend (saturates but associative, no disagreement), Raven sleep-mean (sublinear but associative, mush at depth), and tpr (substructure via explicit roles, unbounded bilinear, associative sum)."*

**§4 searches (subagent, 11 searches, 2026-09-15):**
- "Meld" binding law: no prior art under that name (closest: Swan & Wyble binding pool 2014 — resource sharing, no composition law; bioRxiv 2026.02.18.706604 — compares six binding ops, proposes none; ROSE arXiv:2303.08877 — different machinery). **The paper's own precursor list (§4.9.1, read in PDF) confirms the conjunction-only novelty claim**: VTB (Gosmann & Eliasmith 2019) is non-associative but **non-commutative**; Assembly Calculus Merge (Papadimitriou et al. 2020) creates assemblies without the invariants; sublinear dendritic integration for feature binding is experimentally established (Cazé et al. 2024; Tang et al. 2025); divisive normalization is a standard bounded-integration motif (Carandini & Heeger 2012; Ohshiro et al. 2011); Reimann 2025 shows non-associativity is computationally useful for temporal structure. "Meld... does not introduce a novel physiological primitive; its novelty lies in showing that familiar neural primitives, when jointly constrained by the algebra of the computation they must realize, converge on a previously unarticulated binding law" — bounded + same-space + content-dependent + non-associative + exchange-symmetric + disagreement-sensitive-at-fixed-summed-drive, "no previous neurally interpretable binding law has been derived to satisfy this full conjunction."
- VSA binding ops (Kleyko et al. survey arXiv:2001.11797; VTB Gosmann & Eliasmith 2019): all bilinear/elementwise, invertibility-oriented; **saturation is normalized away, not embraced** — Meld not anticipated.
- TPR (Smolensky 1990; McCoy et al. arXiv:1812.08718): bilinear, role-explicit, linear — not anticipatory, though it satisfies several NAP invariants *via roles* (F3).
- Marcolli–Berwick (arXiv:2305.18278, arXiv:2507.13501): the correction target is real and correctly identified — a *different* law (Rényi-entropy semiring).
- Stable heteroclinic channels (Rabinovich lineage): sequential/workspace transitions only — adjacent, not anticipatory.
- Effective dimensionality vs bracketings: **unclaimed territory** (participation-ratio literature is vast; nobody applies it to separating bracketings).

**Scoring:** Q1 (no prior art) **YES** — published conjunction absent, in-stack conjunction absent (census table). Caveat: sublinear dendritic integration is known biophysics (Poirazi–Mel lineage) — novelty rests on the *composition-law* framing, not the biophysics; and the PDF equations are unverified (⚠ T1). Q2 (new behavior class) **YES** — bracketing-preserving, bounded, disagreement-coding composition is a capability class *no shipped composer has* (all are provably bracketing-blind). Q3 (selling point) **qualified** — sentences finish (F5/F4) but are quality claims ⇒ §3.6 PoC required. Q4 (force multiplier) **YES** — connects tpr, committed_field_blend, Raven consolidation, participation_ratio, jury_disagreement, cgsp, vibe KG (≥2 pillars).

**Verdict: Gain (GOAT).** Super-GOAT gate explicitly held open at Q3 — if Issue 801's PoC confirms hierarchy recovery + PR separation on our substrates, re-run the gate and file the private guide (riir-neuron-db consolidation domain). No "candidate" hedging: the gate is either re-run on PoC evidence or the finding stays GOAT.

---

## 5. Honest caveats

- **PDF read after the initial note** (curl + pdftotext, 2026-09-15): the first-pass reconstruction (Hadamard → L2 → tanh) was wrong in the mixture term — corrected above; the census table's saturation/sublinear columns predate the read and are read as "boundedness/content-dependence" now that the paper's own ablation hierarchy (law 8 ≈ Meld on every axis) is known. Prior-art verdicts unaffected (strengthened by the paper's own precursor section).
- Murphy's domain is neural/cortical plausibility; ours is latent-state engineering. The transfer is the *algebra* (NAC invariants + the extensivity correspondence + the law shape), not the neuroscience. Every NAP mapping is domain-translated (grouping = consolidation batches / span coupling; workspace = shard store / KV; Merge = any binary latent composition).
- **The D_eff readout is not free for deterministic vectors**: our latent states are deterministic f32 (amplitude a_ℓ → power a_ℓ²), exactly the paper's named failure case for `D_eff = exp(S₂)` — PR over squared amplitudes gives exp(3S₄−2S₂). Still separates bracketings, but the PoC must measure the right quantity (PR of the squared-amplitude distribution over atom-direction projections), not assume the clean identification.
- The paper's simulations are nearest-centroid decode on synthetic lexicons (60 lexicons, 0 dB, depth ≤5) — a controlled toy, exactly our §3.6 PoC regime; no claim about noisy real-world latent streams transfers without the PoC.
- The bioRxiv 2026 binding-geometry comparison paper remains the nearest methodological neighbor; read at PoC time.

---

## 6. Disposition

| Artifact | Where | Status |
|---|---|---|
| This note | `katgpt-rs/.research/560_` | filed 2026-09-15 |
| PoC + audit + primitive | `katgpt-rs/.issues/801_nap_meld_composition_audit_poc.md` | open |
| Private consolidation guide | `riir-neuron-db/.research/` | **deferred** behind 801 PoC (Q3 evidence) |
| Healer consumer issue | `riir-clippy/.issues/` | **deferred** behind 801 PoC |
