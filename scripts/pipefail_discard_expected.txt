# Findings pinned by MEMBERSHIP with a REQUIRED reason per row.
# Read by scripts/pipefail_discard_drift_sweep.py.
#
# Format, one row, 3 whitespace-separated parts:
#
#   <repo>:<relpath>:<8-hex sha256 of the stripped line text>#<ordinal>
#     <REASON — why this row exists instead of the fix>
#     #= <the source line the digest was taken from>
#
# The key is LINE-FREE on purpose (arm_reach_survivors_expected precedent):
# a line NUMBER drifts on every edit above it, so a line-numbered pin file
# reds on commits that changed nothing and gets deleted. The ordinal is
# scoped to the whole (repo, rel, digest) address; only genuinely duplicate
# line text shares an address.
#
# The `#=` comment is VERIFIED against the measured line on every run — the
# digest is unreadable, so the comment is the only part of this file a human
# adjudicates from, and a comment nothing can red drifts into a lie.
#
# ── reds in BOTH directions ──────────────────────────────────────────────────
# A new UNPINNED finding is a finding. A pinned row that no longer fires is
# a finding too — the row and the repair belong in the same commit. A count
# would be green on the swap.
#
# ⛔ THE DEFAULT IS TO FIX THE SITE, NOT TO ADD A ROW. Two row classes exist
# here, and each row says which it is:
#   DELIBERATE TRIPWIRE  the death IS the assert (the proof_gate family:
#                        an axiom-free proof must fail loud). Re-home the
#                        assert, then drop the row in that commit.
#   LIVE KILL-SHAPE      an unexamined instance of the class — the script
#                        dies before its own output/report (the
#                        perf_rematch damage shape). The repair is one
#                        neutralizer (`|| true` at the substitution tail or
#                        a trailing `|| cmd`); drop the row in that commit.
#
# Seeded from the first measured run, 2026-09-15, all 20 contract repos:
# 194 tracked .sh · 1359 sites · 53 findings · 162 guarded · 11 local-masked
# · 0 UNPARSED · 0 COND-POS.

# (dropped 2026-09-15: full_gate.sh WASM_RESIDUE got its `|| true` tail in the
# instrument-landing commit — the changed-set case now reaches the != comparison
# instead of dying in grep; and riir-chain teardown.sh:24 got the same tail,
# restoring its already-gone re-run branch.)
#
# ⛔ CORRECTED, same day (Issue 798). The teardown.sh half of that note was
# FALSE when written: the row was dropped here on the strength of a fix that
# did not exist in riir-chain, and this sweep read `✗ UNPINNED finding
# riir-chain:cloudflare/edge-wallet-container/teardown.sh:cc4d29da#1 · FAILED`
# until the real fix landed at riir-chain 5f814a2. Dropping a row for an
# absent fix is the UNRECOVERABLE direction — nothing then points at the
# site. The rule adopted in Issue 798: a cross-repo repair is not landed
# until it is COMMITTED in the sibling repo, and a note here claiming one
# must CITE THE SIBLING COMMIT, as this one now does.
#
# (dropped 2026-09-15: the three money_format_gate.sh reads — coin_vector,
# delta_vector, MAX_SUPPORTED_VERSION — are fixed at riir-chain 41e2de8,
# COMMITTED and pushed, 0/0 vs origin, verified with a two-sided known answer
# on a types.rs with coin_vector removed: at that commit's parent rc 1 with no
# output at all, after it rc 1 with the FAIL line naming the missing field.
# ⛔ The repair there is `|| true` AND an emptiness check, not the neutralizer
# alone: an empty $coin_type never matches 'f32', so `|| true` by itself would
# have turned a silent death into a silent PASS on exactly the regression the
# check exists to catch. Read that before applying the header's one-line
# "repair is one neutralizer" recipe to any row whose captured value is then
# TESTED.)
#
# (dropped 2026-09-15: the three ci_feature_guard.sh passed-test totals are
# fixed at riir-auth 0a8fa91, riir-viewbridge fe1f6ed and riir-dapps 99481c6 —
# all COMMITTED and pushed, 0/0 vs origin. Here the header's one-line recipe IS
# the whole repair, and the sites say why: each awk already prints `s+0`, so the
# empty case has a defined intended value, and each sits ONE line above the
# floor check written to report it and ONE line below a sibling `grep -c
# ... || true` that already carried the neutralizer. Known answer on a TEST_OUT
# with no matching line: without the tail rc 1 and the floor check never runs;
# with it total=0 and the floor check reports it.)
#
# (dropped 2026-09-15, batch 2 — all COMMITTED and pushed, 0/0 vs origin:
# riir-neuron-db 9696cc8, riir-train ab589025, riir-mmorpg-examples dab5d3b
# (three sites: ci_boundary_guard.sh's comment strip, ccu-bench/run-rung.sh's
# process sample, ci_feature_guard.sh's cargo tail), riir-dapps c318dcb (both
# kat-service ETag reads).
#
# ⛔ riir-neuron-db is the specimen that settles how this file's one-line recipe
# should be read, and it is MEASURED, three arms, on a LEAK_OUT with no
# test-result line:
#       at HEAD        rc 1, no output            — silent death
#       `|| true` only rc 0, branch skipped       — the SECURITY GATE PASSES an
#                                                   unverified run
#       both halves    "NO test-result line", rc 1
# The middle arm is the recipe applied verbatim, and it is strictly worse than
# the bug it repairs. That line carried a SECOND defect — it ended `)":`, a
# stray colon concatenated outside the quotes, so `[ -z "$LEAK_LINE" ]` could
# never fire and the fail-loud branch was dead code independently of pipefail.
# Where a captured value is TESTED, read the test before neutralizing the
# capture.)
katgpt-rs:scripts/proof_gate.sh:b6c4ffde#1   DELIBERATE tripwire: death on an empty PrintAxioms.lean is the precondition assert - an axiom-free proof must fail loud here, not pass. Drop the row only in the commit that re-homes the assert.   #= directives="$(grep -c '^#print axioms' PrintAxioms.lean)"
riir-ai:scripts/proof_gate.sh:b6c4ffde#1   DELIBERATE tripwire: death on an empty PrintAxioms.lean is the precondition assert - an axiom-free proof must fail loud here, not pass. Drop the row only in the commit that re-homes the assert.   #= directives="$(grep -c '^#print axioms' PrintAxioms.lean)"

riir-chain:scripts/katgpt_lock_freshness_gate.sh:c8fcede9#1   INERT BY CONSTRUCTION (adjudicated 2026-09-15, not unexamined): the empty case cannot reach this line - `[ -z "$PAIRS" ]` twelve lines above prints the dissolved-hazard notice and exits 0. The residue is a PAIRS of whitespace only, which lock_katgpt_pairs cannot emit (its awk prints `src" "name` and name is non-empty by the ^name = "katgpt- match). The classifier is static and cannot see a guard that is an earlier exit, so the row stays.   #= N_PACKAGES="$(printf '%s\n' "$PAIRS" | grep -c .)"
riir-chain:scripts/katgpt_lock_freshness_gate.sh:4b17da6b#1   INERT BY CONSTRUCTION (adjudicated 2026-09-15, not unexamined): same `[ -z "$PAIRS" ]` early exit as the row above; awk's $1 is the rev field of a line the producer guarantees non-blank, so sort -u | grep -c . is >= 1 whenever this line is reached.   #= N_REVS="$(printf '%s\n' "$PAIRS" | awk '{print $1}' | sort -u | grep -c .)"
riir-chain:scripts/money_bytes_gate.sh:25550db3#1   INERT BY CONSTRUCTION (adjudicated 2026-09-15, not unexamined): $hit is produced by `grep -lwF -- "$fn"` in the same loop's process substitution, so the -owF re-scan of that file with that pattern must match. ⚠ The residue is REAL but is a different class: a concurrent editor deleting the call, or the file, between the two greps - this workspace runs five-plus sessions against shared worktrees (Issue 797). Not repairable with a neutralizer; a `|| true` here would silently record n=0 occurrences for a file that has them.   #= n="$(grep -owF -- "$fn" "$hit" | wc -l | tr -d ' ')"
riir-chain:scripts/proof_gate.sh:b6c4ffde#1   DELIBERATE tripwire: death on an empty PrintAxioms.lean is the precondition assert - an axiom-free proof must fail loud here, not pass. Drop the row only in the commit that re-homes the assert.   #= directives="$(grep -c '^#print axioms' PrintAxioms.lean)"
riir-deployer:cloudflare/control-do/test.sh:b7a83d21#1   LIVE kill-shape: a table with no ts_ms rows kills the e2e before its retry loop; unexamined - fix drops the row.   #= TS_PAIR="$(sq_retry "$DB" "SELECT CAST(value AS TEXT) FROM \"$TABLE\"" | LC_ALL=C grep -o '"ts_ms":[0-9]*' | head -1)"
riir-mmorpg-examples:cloudflare/warm-tier-do/test.sh:16f74227#1   LIVE kill-shape: a header with no next_segment_seq kills the e2e mid-assert; unexamined - fix drops the row.   #= SEQ1=$(printf '%s' "$H1" | grep -o '"next_segment_seq":[0-9]*' | cut -d: -f2)
riir-mmorpg-examples:cloudflare/warm-tier-do/test.sh:3c9eaaee#1   LIVE kill-shape: a header with no next_segment_seq kills the e2e mid-assert; unexamined - fix drops the row.   #= SEQ2=$(printf '%s' "$H2" | grep -o '"next_segment_seq":[0-9]*' | cut -d: -f2)
riir-mmorpg-examples:scripts/ci_boundary_guard.sh:1383d9e5#1   INERT BY CONSTRUCTION (adjudicated 2026-09-15, not unexamined): the enclosing `if grep -qE '^fn distance_2d|^pub.*fn distance_2d' "$file"` is the SAME pattern on the SAME file, so this line is reached only when the symbol is present and the -nE re-scan cannot miss. The pin's original reason had it backwards - the missing-symbol case never reaches here.   #= line=$(grep -nE '^fn distance_2d|^pub.*fn distance_2d' "$file" | head -1 | cut -d: -f1)
riir-mmorpg-examples:scripts/ci_boundary_guard.sh:286bc90c#1   INERT BY CONSTRUCTION (adjudicated 2026-09-15, not unexamined): same shape as the row above - guarded by an `if grep -qE` on the identical pattern and file.   #= line=$(grep -nE '^fn distance.*sq|^pub.*fn distance.*sq' "$file" | head -1 | cut -d: -f1)
riir-mmorpg-examples:scripts/ci_boundary_guard.sh:eda57629#1   INERT BY CONSTRUCTION (adjudicated 2026-09-15, not unexamined): same shape again - guarded by an `if grep -qE` on the identical const alternation and file.   #= line=$(grep -nE 'const\s+(FEAR_KICK|FLEE_FEAR|NPC_FEAR_DECAY|EMOTION_KICK|THREAT_KICK)' "$file" | head -1 | cut -d: -f1)
riir-neuron-db:scripts/proof_gate.sh:b6c4ffde#1   DELIBERATE tripwire: death on an empty PrintAxioms.lean is the precondition assert - an axiom-free proof must fail loud here, not pass. Drop the row only in the commit that re-homes the assert.   #= directives="$(grep -c '^#print axioms' PrintAxioms.lean)"
