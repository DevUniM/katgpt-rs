# Sweeps deliberately NOT calling sweep_advisory() — Issue 797 T5.
#
# One row per sweep, `name  # reason`. A reasonless row is REFUSED, and a row
# whose sweep has since been wired REDS: a pin file that only ever loosens is a
# backlog wearing a pin (Issue 785).
#
# Deliberately EMPTY. Every sweep in the family reads the working tree, so
# every one of them has the Issue-797 exposure; a row here would be saying
# "this sweep may report a finding no commit contains, and that is fine".
