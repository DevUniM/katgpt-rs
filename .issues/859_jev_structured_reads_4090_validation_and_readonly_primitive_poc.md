# Issue 859 — Jev structured reads: 4090 open-reference validation + read-only seeded-canvas read primitive POC

**Status:** OPEN — research-derived POC (Research 574; the vLLM open reference of the Jev contract Research 562/573 track). Owner-routed to the 4090. **T0+T2 DONE, T3+T4 LANDED 2026-09-20 (Bench 816: GOAT G1/G1b/G2/G3/G4 PASS, opt-in `structured_reads`), T5 PARTIAL (enabler landed; measurement NOW UNBLOCKED — see T1), T1 DONE 2026-09-20 (executed by the yielded-to session; Research 574 §8 — no marlin garble, corpora 10/10 · 10/10 · 9/12-corrected, the video-profiler memory finding).** Issue stays open pending T5's measurement + the promotion decision (T6: numbers landed, verdict §8+§6).

## Why

vLLM PR #57250 (unmerged, `ready`) turns DiffusionGemma into a Jev-style structured-decision server: seed canvas → ONE denoise step → argmax + exact label-id logprobs at free slots → **skip the commit forward**; entropy-gated re-reads ship mean ± bars. Research 574's grep: the **composition is zero-prior-art in-workspace while every component ships** (`canvas_schema` masks, `ac_prefix::conditional_logprob`, `SamplerFeatures` slot stats, D2F denoise stack, `margin_gate`, CalibratedSigmoidGate/810). Two deliverables: (A) validate the open reference on our silicon — numbers as *intel* for Research 573 / riir-clippy Issue 125, never league evidence (different model class + silicon; league absolute-numbers rule); (B) POC the read-only primitive on OUR D2F stack.

## Pinned claim (defend-wrong shape)

`structured_read()` = seed(canvas: fixed schema tokens + single-token free slots) → one denoise step → per-free-slot {argmax, logprob over caller-supplied label-id list, entropy} → return; **no commit forward**. Distinguished from `ac_prefix` (commits; scores for AR drafting) and `diffusion_sampler` (remask confidence inside generation, not a request).

## Tasks

- [x] 0. Probe the 4090 (Tailscale `100.85.179.44` when remote; LAN timed out this session): reachability, vLLM install/version, free VRAM, no sibling compute job (GPU-exclusivity rule) — **DONE 2026-09-20**: LAN down (as predicted), Tailscale reachable; **16,839/24,564 MiB used, 2% util, sibling bench_989_tap_crosscheck LIVE** ⇒ exclusivity blocks T1; Python 3.10.11 present; full results in Research 574 §5
- [x] 1. **Reference validation**: clone `mmastrac/vllm@structured-reads-main` (PIN sha at clone), serve `nvidia/diffusiongemma-26B-A4B-it-NVFP4` (`--diffusion-config '{"canvas_length": 32}'`); if the SM89 NVFP4-marlin bf16 garble fires → `--dtype float16`; run `examples/features/diffusion_reads/structured_server.py`; reproduce the programming-language + unit-comparison corpora; record req/s + accuracy + reads-per-decision on OUR box into Research 574 (intel table, separate from league cells) — **DONE 2026-09-20 by the yielded-to session (Research 574 §8, full table + deviations)**: head `ceb8eebf` re-pinned; python-only overlay onto `vllm/vllm-openai:nightly` (PR is 100% Python — skips §7's build paths entirely, fresher than the openjev pin); **garble did NOT fire (bf16 kept)**; smoke on the PR's own outage prompt matches DGX (urgent 0.881±0.042 vs 0.88±0.04, bucket 0.9986, furious 3.00, reads=4); corpora prog-lang **10/10**, human-lang **10/10** (own items), unit-comp 7/12 raw → **9/12 corrected** (2 of the misses were the executor's mislabeled items — the model was right; the 3 true misses are the same unit-conversion class as the PR's 2); throughput under the degraded config (eager/sync/seqs-16): **5.2 / 14.3 / 20.8 req/s** at 1/8/16-way vs PR's 8.7/54. **Memory finding (reusable)**: the mm VIDEO profiler reserves ~4.4 GiB — KV = −0.49 GiB at every knob combo until `--limit-mm-per-prompt '{"image":0,"video":0}'`; then 18.15 GiB weights serve beside a resident GUI at util 0.92. Restorable stack on E: (`sr859-overlay:ceb8eebf` + hf-cache + fork + corpora.py + results.json).
- [x] 2. **Substrate read (substrate-first, before any code)**: `katgpt-forward` `denoise_loops.rs`/`d2f_verifier.rs`/`diffusion_sampler.rs` + `katgpt-core` `canvas/` + `ac_prefix/` — map seed/read/skip-commit onto them; write the mapping row into Research 574 before implementing — **DONE 2026-09-20: Research 574 §5. Key resolutions: seed = the mask-token placement convention (AcPrefix-SHAPED, not consumed — the D2F bidirectional forward is natively the right conditioning shape; `canvas_schema` is positions-not-tokens and NOT consumed); one step = `forward_bidirectional_positions_into` (the identical call `denoise_loop` makes); read = full-marginal label logprobs + NEW subset-normalized entropy; skip-commit = never write `tokens[p]` (contrast `denoise_loops.rs:188`). T5 note: our forward is deterministic ⇒ re-reads are bit-identical; agreement bars need the sampling re-read helper (temperature > 0).**
- [x] 3. **POC primitive** behind a feature flag: `structured_read()` per the pinned claim; label-id list API (the M2 lesson — never top-k); entropy over the NORMALIZED label distribution (the PR reviewer's nit is our house rule) — **LANDED 2026-09-20**: `katgpt-forward` feature `structured_reads = ["dllm"]`, module `src/structured_read.rs`. `structured_read_into` (alloc-free core: fixed `[f32; 64]` readouts, reusable scratch, MAX_LABELS=64 bounded-domain cap, fail-closed validation) + `structured_read` convenience + `sample_label_index` (the T5 stochastic re-read enabler). Zero new substrate — composition only, per the §5 mapping.
- [x] 4. **GOAT gates** — **PASS (Bench 816)**: G1 full-marginal exactness (f64 cross-check <1e-5 + bit-identity across calls) · G1b canvas bit-identical (no commit) · G2 read/full-loop median ratio **0.4588** (33 interleaved pairs ×8, Issue-723 discipline, release) · G3 **154/154** existing suite green · G4 **0 allocs** across 32 reads (canary-armed, `--release --features structured_reads,alloc_tracking`). Three-competitor arm: step-1 read vs full-loop-then-parse 4/4 agreement after 1 commit round (informational — random weights carry no ground truth; accuracy rides T1).
- [-] 5. Entropy-gated re-read policy arm (M3: H1 > τ → N re-reads, mean ± bars) — measure whether agreement bars beat single-read entropy as a confidence proxy on the fixture corpus — **PARTIAL: the ENABLER landed (`sample_label_index`); measurement UNBLOCKED by T1** (the reference protocol is now measured on our silicon — reads=4 fires exactly where H1 > 0.1, §8 — so the threshold family has a live comparator; ground truth = the §8 corpora or the `micro_dllm_text` trained fixture).
- [ ] 6. Numbers + verdict into Research 574; if GOAT → plan for promotion decision; close this issue under the noise-reduction rule with the commit hash — **numbers LANDED (§8 the 4090 cells + §6 the POC verdict); remaining: T5's measurement → then the promotion decision** (the GOAT is a POC gate, not a promotion gate — promotion needs the accuracy axis, which T5's measurement now has a path to).

## Fallbacks / risks

- NVFP4-marlin on SM89 = workaround path (known bf16 bug; float16 dtype); if unrunnable: skip T1 (defer reference validation to a fp8/fp16 community quant or `pst2154/Nemotron_Jev` dense variant becoming runnable) and do T2–T5 on CPU/small fixtures.
- PR is UNMERGED and moving (5 prerequisite PRs split out, now numbered: #57414/#57416/#57417/#57462/#57589; head `ceb8eebf` API-read 2026-09-20, was `58aacf2`) — re-pin at clone; the API surface (`vllm_xargs`) is provisional upstream (openjev pins the fork for that reason).
- DGX Spark numbers are NOT headroom evidence for us; only same-box cells from T1 count as intel.
- Do not touch the perf league: no cell in `perf_rematch_league.md` moves for a diffusion-LM decision mode.
