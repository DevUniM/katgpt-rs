# Issue 841 — Cactus/Needle sweep follow-ups (katgpt-rs-owned leads)

> ⚠ **Renumbered 840 → 841** (2026-09-18): dual allocation — a concurrent session holds `.issues/840` upstream, itself a record of this same session's 569 research dual-allocation (`cf6eb54f0`…`a2ff2a6f5`); the upstream holder keeps 840, this note lands as 841.

**Status:** OPEN — 5 of 14 rows closed (`.kpt` POC, fix_verify refusal audit, riir-rag embedder identity, grammar-forced vocab-projection skip — the one PROMOTED to default-on — and KV sinks + bounded window), 1 closed 2026-09-19 as substrate with its wiring follow-up open (calibration staleness). Leads consolidated from Research 568/569/570/571 (22-source Cactus Compute sweep, 2026-09-18). One checkbox per lead; `- [-]` = deliberately deferred with reason. Cross-repo training leads live in `../riir-train/.issues/561_cactus_training_leads.md`. **2026-09-19 owner call (gate D3): the `.kpt` row is DONE as a POC** — example + tests (poc/perf/sec bad-injection) landed behind opt-in `kpt_archive` ([Bench 841](../.benchmarks/841_kpt_archive_poc.md)); the format+engine lift itself stays parked until a NeuronShard-Merkle consumer wants single-file mmap hot-swap.

## Primitive / league lane (katgpt-rs + riir-gpu consumers)

> ⚠ **Citation repair 2026-09-19.** The CQ-W2A8 row read *"dot through Plan 431 LUT"*. This repo's Plan 431 is the **Cross-Stage Residual Relocation Operator** — a real, local, unrelated document, so the citation was FOLLOWABLE TO THE WRONG PLACE, which is the one class worse than an unfollowable one. The LUT meant is **Plan 452** (SIMD LUT DeQuantization, `dequant_dot_via_lut`, COMPLETE + promoted default-on). The head-to-head baseline named beside it, `gemv_q4_k_row_lut`, has **no definition in this repo at all** — it is riir-ai's, and Plan 486 is riir-ai's too (this repo has no 486). Verified by grep, not remembered.

- [ ] **CQ-W2A8 never-unpack GEMV** (Research 568 fusion 1): rotate each 128-lane activation group via existing `hadamard_transform_inplace()`, int8 per-group scale, dot through the Plan 452 `dequant_dot_via_lut` LUT with int8 accumulate + one per-group scale multiply. Flag `cq_w2a8`; head-to-head vs **riir-ai** Plan 486 `gemv_q4_k_row_lut` (`riir-ai/crates/riir-infer-core/src/quant/q4k.rs` — the baseline is a SIBLING's, so this row cannot be gated inside this repo alone); measure the per-group FWHT amortization claim, don't assume it.
- [ ] **CLAWS activation-sparse decode cell** (Research 571 §C-15): per-neuron Fisher-style `cᵢ` LUT (offline calibration) × GeGLU gate scores → `argtopk_with_scratch` mask → Q4_K GEMV skips masked K-superblocks. ⚠ re-run x86_64 execution matrix (Bench 806 argtopk class). Bar: dense-level quality at 50% density, ≥1.2× MLP block.
- [ ] **HiDRA-v2 on the Bench 688 permanent negative** (Research 571 §C-16): LDA median-split tree init (closed-form, modelless arm FIRST) → beam routing + truncated scoring; reuse Issue-666 `ClusterLayout` packed rows; gate on the existing 123-probe real-checkpoint harness. riir-train BCE per-node training only if the modelless arm stalls. Verify router-storage cost (≈V·d, same size as head) before claiming a win.
- [ ] **TurboQuant-H riir-rag index lane** (Research 571 §B-11): 2.125-bpw Hadamard + per-group Lloyd-Max over the embedding index via `group_lut_at` 4-entry LUTs. **Gate = recall@k vs f32, not PPL.** Open question: quantize-then-wedge vs wedge-then-quantize vs Clifford-wedge KNN.
- [x] **KV permanent sinks + bounded window** (Research 571 §B-8) — **DONE 2026-09-19, GOAT 5/5, NOT promoted (blocked by a missing corpus, not a failing gate).** `katgpt-core::kv_sink_window` behind opt-in `kv_sink_window` (requires `usage_rate_eviction`): `SinkWindowPolicy { n_sink, window }` + `SlotClass` + `WindowFidelity` + `select_evict_windowed` + `sink_pin_mask_into`. 20 arms + [Bench 841](../.benchmarks/841_kv_sink_window_goat.md) — G1 the ceiling holds exactly (peak live **260 of 260** over 20 000 steps against a scorer that ranks the sinks worst, 0 breach, 0 sink loss, steady state AT the ceiling), G2 the bounded arm is **flat: 166 ns/step at every sequence length, 1.00× over 16× growth**, against unbounded 7 µs → 100 µs (601× at seq 16 000), G3 `UNBOUNDED` reduces to `select_evict_into` **index for index**, G4 **0 allocs** over 2 000 steady-state steps, G5 the fidelity verdict refuses in the permissive direction.
  - ⛔ **NOT promoted, and the reason is a RULE not caution:** with an unknown `d_max` this is a lossy KV surface, and `kv_eviction`'s own docs require the Plan-585 `runaway_gate` on a **sealed long-context eval** before any lossy policy goes default-on (aggregate perplexity is flat while output length runs to the cap — 35–128× blowups with SubEM reading fine). The instrument already ships; **the corpus is the open task.** Blocked by a missing measurement, not a failing one.
  - ⚑ **A bounded window is NOT automatically lossy, and finding that is what makes this more than a re-implementation.** `katgpt-attn::alibi_entmax_window_1p5` (Issue 747, Research 549) derives a `d_max` beyond which entmax-1.5 mass is **exactly** zero for an ALiBi head. `window >= d_max` ⇒ `WindowFidelity::Lossless` — bit-identical output, not a lossy surface, runaway gate does not apply. That is a separate, later promotion with a *different* argument (bit-identity, not a corpus). `d_max` is caller-supplied because katgpt-attn is DOWNSTREAM of katgpt-core.
  - ⚑ **`w07` found a real defect at the module's own sentinel:** `UNBOUNDED` is `window: usize::MAX`, and a literal `distance < window` classified exactly one representable distance (position 0 at `current_pos = u64::MAX`) as `Stale` — under the policy that means "nothing is stale", because `usize::MAX == u64::MAX` on 64-bit and a window of `w` covers `0..w`. Found by an arm, not by reasoning.
  - [ ] **Follow-up:** the sealed long-context eval (the corpus for `runaway_gate`) — the one thing standing between this and a promotion. **FILED as [Issue 857](857_sealed_long_context_eval_for_the_runaway_gate.md)**, whose first task was the boundary question (the corpus is DATA and a generation harness needs a model this repo does not own). ⚑ **ANSWERED 2026-09-19, and it reclassifies this row: 857 is PULL-GATED, not blocked, and the corpus is not the binding blocker anyway.** The corpus + harness is riir-train's (`kimi_k3_long_context.rs` already packs long windows there), the canary wiring is riir-ai's and is already **riir-ai `.issues/882` T2**, and only `RunawayStats` crosses back. 882's own consumer verification found **zero attention-KV eviction surfaces in riir-ai**, so there is nothing to run a runaway canary against — and the no-default-consumer rule keeps `kv_sink_window` opt-in regardless of any corpus, because nothing in this repo calls `select_evict_windowed`. Build the corpus at 882's T-Pull-1, sized to THAT consumer's window rather than to this policy's default 260. Also unwired: a real KV cache consumer and a mass producer, both consumer-side per `kv_eviction`'s documented boundary. Fuse with KVarN; league + riir-esp32 relevant.
  - ⛔ **Substrate correction 2026-09-19: this row said "Grep-clean today (nothing ships)" and that is WRONG in the direction that invites a parallel system.** `katgpt-core::kv_eviction` ships (Plan 585, Research 523): usage-rate `mass/age` scoring, `UsageScoreTable`, `select_evict_into(scores, k, pinned: &[bool], out)` — **which already takes a `pinned` mask**, i.e. the non-evicting-sink seam exists — plus `RunawayStats` / `runaway_gate`, the promotion gate any lossy KV policy must pass. `katgpt-attn::dash_attn::eviction_window` also ships. What is genuinely absent is **`sink`** as a concept (zero literal hits in either) and the bounded window's deterministic ceiling. So the work is *consume `select_evict_into`'s pin set + add the window*, not build an eviction policy. Verified by grep at the time of writing, not remembered.
- [x] **Grammar-forced vocab-projection skip** (Research 571 §B-8) — **DONE 2026-09-19, GOAT 5/5, PROMOTED to default-on.** `legal_token_set`: `katgpt-core::legal_token_set` (`CsrLegalSet` + `ProjectionPlan` + `RestrictionPolicy`) + two default-`None` `ConstraintPruner` hooks + `LodestarPruner` enumeration + the `build_dd_tree_lodestar` fast path + `katgpt-forward::restricted_lm_head`. 38 arms + [Bench 841](../.benchmarks/841_legal_token_set_goat.md) — G1 bit-identical over 12 cells, G2 **20.6–22.5×** tree build at grammar degrees 8/64, G2b crossover **measured** at ~20% active, G3 worst case 1.3–3.5× *faster*, G4 0 added allocs, G5 index memory +0.01% for a real grammar.
  - ⛔ **Substrate-first verdict: BOTH halves already shipped, privately, and nothing joined them.** `LodestarAutomaton` holds δ as a dense `n_states × vocab_size` table naming every legal token of every state and exposed only the per-token point query `transition(s, t)` — the row scan existed in two private precompute helpers and **threw the token ids away**. `katgpt-forward::cluster_head::fill_cluster_exact` is literally *score only these vocabulary rows* and was `pub(crate)`, reachable only through a cluster-shaped API that picks the row list itself. So nothing was built: `CsrLegalSet` is generalised from this repo's one shipped state→successor enumerator (`bisimulation::graph::TransitionGraph`, `adjacency_window` + `for_each_adjacent`, CSR + callback, allocation-free) over token ids instead of operator labels, and `restricted_lm_head` is a public wrapper over the existing gather, not a second SIMD dot.
  - ⚑ **The lead's framing was one level off, and the in-repo cost was larger than it said.** The paper claim is about skipping lm-head ROWS. The shipped cost here is the `is_valid` scan in `build_dd_tree_lodestar`, which calls `follow_path(parent_tokens)` **inside every one of 32 768 predicate calls**, so a heap pop cost O(vocab × prefix) — the builder already carried a comment about exactly this shape for `ln()` and had cached that, with the validity scan uncached beside it. Both halves are addressed; the tree half is the one with a measured 20× and an in-repo caller.
  - ⛔ **The naive rule is wrong and the bench refuses it.** "Smaller than the vocabulary, therefore gather" loses above ~20% active — gathered LM-head rows run at ~20.6 GB/s against ~108 GB/s dense. The crossover is DATA (`RestrictionPolicy::max_active_fraction`, default 0.10) and G2b gates the **shipped default**, not the best cell. ⚑ The measured ~20% independently reproduces Issue 661's 21–34% band from a different kernel and a different caller.
  - **Promoted** because `lodestar` is itself default-on, so the seam improves a path that already ships, and the cost side is measured rather than argued (G5: density/2, +41.7% at full density, +0.01% for a grammar). Feature counts 620→621 total, 203→204 default-on.
  - [ ] **Follow-up:** the hooks default to `None`, so `LodestarPruner` is the only implementor today — `VocabChannelPruner` (whose `VocabChannelMap::layer_union` is already a sorted token set) is the obvious next one. And `restricted_lm_head` is exported + tested with **no in-repo caller**: a real forward pass with a grammar attached is riir-ai-side, since that is where the decode loop lives.
- [ ] **functional_embed (PETE-distill)** (Research 571 §C-13): Fourier-of-token-ID features `T(p)` + closed-form ridge/ALS distillation of shipped `wte` via KARC — zero GD. Gates: ≥0.98 mean cosine on Bonsai table + riir-rag recall + decode. Free first step: permute-tokens ablation on our BPE tokenizer.
- [ ] **SAN league cell** (Research 570): gated on an artifact — convert a shipped SAN checkpoint (paper ships checkpoints) or serve `needle3.cact`; flag `san_decode`; bytes/token-at-matched-params comparison vs llama.cpp.

## Fusion ideas, novelty-TBD (issue-first per the gate — do NOT promote without §4 searches)

- [x] **`.kpt` archive** (Research 568 fusion 2): .cact-style nameless positional layer-major single-file mmap × NeuronShard Pod × **BLAKE3/Merkle integrity .cact lacks**; atomic weight hot-swap = file replacement. **DONE AS POC 2026-09-19 (owner gate D3: build the POC + perf + sec evidence instead of deferring): `katgpt-core::kpt_archive` (opt-in `kpt_archive`) + `examples/kpt_archive_poc.rs` + `tests/kpt_archive_sec_injection.rs` (17 bad-injection arms, all green). Measured: 4.52 MiB / 8 layers @768×3072, full verify 1006 MiB/s, atomic hot-swap 21 ms, bit-identical round-trip, byte-deterministic build — [Bench 841](../.benchmarks/841_kpt_archive_poc.md). The production format+engine lift (mmap + a NeuronShard Pod consumer) stays PARKED on the original trigger: a consumer that wants single-file mmap hot-swap.**
- [ ] **Engram-delta as the modelless fine-tune** (Research 571 §B-8 fusion 1): write product tool-vocabulary into `StagingEngramTable` → commit → `EngramHotSwap` → freeze envelope → chain-commit. GOAT gate vs LoRA fine-tune (accuracy/latency/training-cost=0). Prior-art class: kNN-LM/RETRO/product-key memories — pin the claim to *zero-training specialization + committed deltas* before any tier claim.
- [ ] **SAN npc_brain v2** (Research 571 §C-12 fusion 1): trained tiny attention-only brain (SAN recipe: QK-norm + sandwich norm + Muon) on trace-dense corpora; knowledge stays context-side per the query-deficit finding. Blocked on a multi-layer brain consumer.

## Boundary / hygiene (cheap, boundary-shaped)

- [x] **Calibration-staleness rule at the freeze/thaw seam** (Research 571 §B-3) — **DONE 2026-09-19, GOAT gate green, NOT promoted.** `katgpt-core::calibration_staleness` behind opt-in `calibration_staleness`: `SnapshotBound<T>` binds a head to the `SnapshotId { version, commitment }` it was fitted under and returns `None` + one loud warning per binding once that identity moves. 16 arms + [Bench 841](../.benchmarks/841_calibration_staleness_goat.md) — G1 all four predicate arms, G3 bit-identical over 1001 swept inputs, G2 guard **1.155 ns/call** vs apply(sigmoid) **1.469** (ratio 0.786, budget ≤ 1.00), G4 **0 allocs**.
  - **Substrate-first verdict:** no calibration-staleness mechanism existed anywhere (searched `stale`/`invalidate`/`epoch`/`generation`/`snapshot_id`/`freeze_id`/`hot_swap`/`provenance`/`dirty` — `epoch`, `generation`, `snapshot_id` and `freeze_id` return ZERO). The predicate is therefore **generalised, not invented**: `DefaultMpiRouterSnapshotHook::cache_valid` (katgpt-spectral) is the one shipped version-keyed invalidation at a swap boundary, and both of its rules are kept with an arm each — generation `0` is never fresh (a `0`-vs-`0` pair compares EQUAL, so an equality-first predicate reads clean at exactly the unwired call site this exists for), and the commitment is re-checked under a held generation to catch a swap that did not bump, reported as its OWN verdict because that repair is a broken bump site and not a refit.
  - ⛔ **The trap a naive adoption falls into, pinned as an arm (`t14`) rather than prose:** `SWTF_VERSION` / `SDBF_VERSION` are **format** versions — `1` for every snapshot ever written. A `SnapshotId` built from one holds the generation constant, the version half can never fire, and the guard degrades **silently** to the commitment check alone. The three shipped envelopes (`TrajectoryFreezeEnvelope`, `CochainFreezeEnvelope`, `FrozenDirectionBank`) carry no generation ordinal at all; `CochainFreezeEnvelope` carries no version field of any kind. The real generation lives in `FuncAttnWeightsSnapshot::version` (katgpt-attn) and `MicroRecurrentKernelSnapshot::version`.
  - ⚠ **NOT promoted, and the reason is not caution:** the primitive guards nothing until a seam is wired, and wiring one changes a shipped signature to `Option`. Four seams named + addressed in the bench doc — `FuncAttnSnapshotStore::swap` (`freeze_thaw.rs:262`, the only one that can supply a real `SnapshotId` today), `StaticCalTable::get_scale`, `CalibratedVerifier::verify_embedding`, and `ConformalIntervalCalibrator`, which is **default-on** and therefore the widest-reach seam and the one whose wiring owes its own gate. Neither katgpt-attn nor katgpt-claim depends on `log`, which is why the warning fires in katgpt-core.
  - [ ] **Follow-up (owner call):** wire the four seams, widest-reach one last. Until then the rule exists and enforces nothing — this row is DONE as substrate, not as coverage.
    - ⚑ **Design settled 2026-09-19, and it dissolves the stated blocker.** The bench doc gives the reason for deferring as *"wiring one changes a shipped signature to `Option`"*. It does not have to. The guarded read is an **additive sibling**, never a replacement:
      1. **Supply first, or the other three have nothing to bind to.** A `SnapshotIdentity` trait in `katgpt-core` (`fn snapshot_id(&self) -> SnapshotId`), implemented for `FuncAttnWeightsSnapshot` in katgpt-attn — which already carries `version: u64` **and** a BLAKE3 commitment, verified at `swap` time. Purely additive, no signature moves. ⛔ The trap the primitive's own `t14` arm pins applies here and nowhere else in the set: populate `version` from the **snapshot generation**, never from `SWTF_VERSION`/`SDBF_VERSION`, which are FORMAT versions and are `1` for every snapshot ever written.
      2. **`StaticCalTable`** gains a `fitted_under: SnapshotId` field (defaulting to `UNVERSIONED`, i.e. never fresh — the unwired caller is exactly who this exists for), a `bind_to()`, and a `get_scale_checked(&current) -> Option<f32>` **beside** the existing `get_scale`, which is untouched. It already owns a `commitment` over its scales; what it lacks is the identity of the WEIGHTS it was fitted against, which is the thing that goes stale.
      3. **`CalibratedVerifier`** the same shape — it already exposes `commitment()`, so only the binding and a `verify_embedding_checked() -> Option<Verdict>` are new. Its own module doc already argues the staleness and ships no mechanism.
      4. **`ConformalIntervalCalibrator` LAST**, because it is **default-on** and therefore the widest-reach seam; its wiring owes its own GOAT gate rather than riding this one.
      All four behind the existing opt-in `calibration_staleness`, so a default build stays byte-identical — which the primitive's G3 already measured over 1001 swept inputs. ⚠ Promotion is still governed by the no-default-consumer rule: seams 1–3 are opt-in consumers and do not earn it; seam 4 is the only one that could, and only with its own gate.
  - ⚠ Still open from the original lead: the UQ "Report the Floor" pairing (discrete-decision corollary — beat a base-rate/Platt floor on ECE + selective-risk@coverage). Untouched; staleness and floor-reporting are orthogonal.
- [x] **fix_verify refusal-vs-truncation audit** (Research 571 §B-9) — **VERDICT: the hazard was LIVE, fixed in riir-clippy `651728c0` (2026-09-19).** A killed mid-verify re-check COULD read as a verified keep, in both compile-oracle decision consumers:
  - **Mechanism:** `CheckRunner::run_check`'s `Err` arm fires only when cargo fails with no JSON line at all; a cargo process killed mid-run (OOM / user kill / disk-full rustc abort) that already flushed `compiler-artifact` JSON comes back `Ok` with `exit_ok = false` + a partial stream — `is_json_line` (a bare `starts_with('{')`) lets it past the Err arm. From the JSON alone that is indistinguishable from a clean pass. `fix_verify::verify`'s loop then merged zero error locations → empty `error_lines` → break → every still-applied edit tallied as kept. The baseline refused the same shape (`!exit_ok`) — the asymmetry was the tell.
  - **Same family, worse-shaped:** the `rustc_errors` driver's `check_all` — a killed initial check read `report.clean = true` (nothing to fix), and a killed recheck read `(0, 0)` on the riir-clippy Issue-100 lexicographic keep rule, the maximum decrease, keeping a fix nothing verified.
  - **Repair (one shared mechanism):** `CheckVerdict::check_verdict(exit_ok, has_error_diags)` in `compile_oracle` — `Incomplete` = non-zero exit with zero attributable diagnostics = refusal (restore everything + loud error, never a keep). Wired into both consumers; both driver Err paths already restored + reported. The completion-sentinel port the lead asked for: the check's exit status IS the sentinel.
  - **Already safe (audited, no change):** the clippy oracle lane (`bin/heal.rs` — `exit_ok=false` → no verdict, Heuristic, never Certified); `ClippyVerifier` (riir-clippy Issue-112 probe witness refuses an untrustworthy run); `rust_perf` verifier (riir-clippy Issue-112 keyed-package refusal; killed → `test_passes = false`); `measure_composite` (its `parse_unsafe` flag already encodes the shape). **Parent-kill residual stays documented** (fix_verify module docs): Ctrl-C mid-verify leaves exactly what `--fix --write` would have written — never worse than the non-verify path, but unverified.
  - **Tests:** verdict matrix (4 cells) + a kill-path e2e with a fake cargo (recheck emits one artifact line + exit 1 → asserts refusal + restore + kept 0; runs in the normal suite, no `#[ignore]`) + both real-cargo acceptance e2es re-run green.
- [x] **riir-rag embedder-identity fingerprint** (Research 571 §B-4) — **VERIFIED 2026-09-19, three findings:**
  - **code_triples path: correct as-is.** `index_code_triples` is syn AST → raw text triples (`CodeTripleExtractor`, deterministic, embedder-free), so `index_code_triples_if_changed`'s `BLAKE3(source)` fingerprint is the COMPLETE identity for that path — an embedder swap cannot stale it because no embedder exists there.
  - **The identity substrate already ships** — riir-neuron-db `EmbedProvenance { text_hash, model_id, translator_id }` + `EmbedProvenanceIndex` + `TranslatorCalibration` (Utopia timestamp lesson; Issue 614 E3 calibration-gated admission for translated rows). `model_id` is BLAKE3 of whatever identity string the caller deems behavior-changing.
  - **The residual is WIRING, not substrate (riir-ai-owned follow-up):** `Embedder`/`DualEmbedder`/`DenseEmbedder` expose no identity string; `index_chunks` / `index_chunks_dense` record no provenance rows; `DenseEmbedIndex::from_mmap` reload validates no model identity. An embedder swap + incremental dense re-index therefore mixes model-A/B vectors in one cosine space SILENTLY. Fix shape: `fn model_id(&self) -> &str` on the embedder traits → record `EmbedProvenance::of(text, model)` beside each dense insert → refuse-or-warn-loud on `model_id` mismatch at mmap attach. All three pieces have shipped substrate; only the retriever-side wiring is missing. **FILED 2026-09-19 as riir-ai `.issues/983` (`78994fa78`, pushed)** — with one correction this note got wrong: the third piece is **riir-neuron-db-owned**, not riir-ai's. `DenseEmbedIndex::from_mmap` has nowhere to *put* an identity — that format is documented *"no header, no framing"* (riir-neuron-db `src/dense_embed/index.rs:290`), so it is a FORMAT change in that repo and not a field riir-ai can add. Re-measured at filing against riir-ai `9e52d2bfb`, not inherited from this row.

## Deferred

- [-] Ships-manifest per-platform gate (Research 571 §B-5 F1): real, but this repo's lane is primitives; `wasm32_surface_audit` covers the load-bearing half. Park until a deploy surface asks for it.
- [-] WASI Preview 2 component target (Research 571 §B-5 F3): zero host consumers; revisit if riir-esp32 or an edge host materializes.
- [-] KV-cache QAT (Research 571 §B-8): riir-train seed — lives in riir-train issue 561.
