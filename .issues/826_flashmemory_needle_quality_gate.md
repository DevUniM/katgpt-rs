# Issue 826: FlashMemory promotion gate needs a needle-axis quality gate (Research 567, arXiv:2609.13141 evidence)

**Status:** T1+T2 MEASURED 2026-09-18 (Bench 814) — relative needle axis PASSED with pinned tolerance `cosQ ≥ 0.99` + 12/12 agreement + pw_rank ±5%; absolute axis UNTESTABLE on the 0.4B testbed (dense oracle never retrieves — measured); 64K priced out (~9h/arm, k_c/v_c re-up-projection is the known Phase-6 cost), follow-up file below. T3 wiring: the promotion gate gains the Bench-814 row (relative pin) — see T3.

**Source:** [Research 567](../.research/567_SAS_LogGate_LM_Loss_Sparse_Context_Ranking.md) — SAS (arXiv:2609.13141) states as a LIMITATION what our shipped corridor has never measured: pooling-based block summaries **destroy needle-like localized information**. Their RULER numbers at 128K / budget-4096: SAS 21.87/23.46/29.95 (4B/8B/14B) vs full attention 63.81/73.87/82.23 — even after retraining the selector on 64K sequences (0.5B tokens).

**Why it applies to us:** `flashmemory_sparse.rs` builds block centroids via `rebuild_from_cache` as the **mean of compressed KV latent** (`c_kv` mean over the block) — the same pooling class AttnGate uses (pooled per-block key statistics). The corridor is perf-validated (Bench 671: 1.8× decode at 64K on 4090) and its mechanism test (Issue 584 Phase 1 Q4) checked "sparse forward preserves accuracy" — but no needle/long-context retrieval axis was measured. SAS's tight-budget data also bounds where the modelless selector is safe: training-free ranking collapses on hard tasks at tight budgets (Quest = 0 on AIME24/25 at 2048, 40.74 on MATH500-4B vs trained-selector 93.47).

**Game-runtime framing (priority #1 surface):** the league's sparse-decode lane is what long-context NPC serving (dialogue histories, world-event transcripts) would lean on. Promotion without a needle gate would ship the exact failure axis SAS documented externally.

## Tasks

- [x] **T1 — Needle-quality bench for `flashmemory_sparse`.** LANDED 2026-09-18 as `benches/bench_685_flashmemory_full_model_niah_qa.rs` + [Bench 814](../.benchmarks/814_flashmemory_full_model_niah_qa.md): full-model (8-layer Kimi-K3-0.40B) dense-vs-flashmemory NIAH, single+multi-needle, ladder 2048→17322 tokens (4096 trained anchor + 2×/4.2× extrapolation rungs). Result: **sparse within tolerance of dense at every rung and both σ arms** — decode agreement 12/12, cosQ=1.0000, pw_rank within ±3%, including σ=0.7 at ~47% coverage / 50-56% recency-fallback. **Tolerance pin (the Phase 3 gate parameter): `cosQ ≥ 0.99` AND decode agreement 12/12 AND pw_rank within ±5% at σ=0.5** — measured margin is 4+ orders of magnitude on the cosine axis. Parity gate (σ=0 + fresh selection ≡ dense) PASSES via the `FFM_PROBE=1` bisect; the probe's first failure was ROOT-CAUSED to refresh-window staleness in the gate's own construction (σ=0 with refresh=64 misses the newest <64 tokens — paper semantics, not a defect), NOT a flashmemory contract failure.
- [x] **T2 — Budget-stress axis.** LANDED same bench: e2e QA holds to σ=0.70 (tightest arm: 12/12, cos=1.0000 at 43.8-50% coverage). Selection-axis sweep (query-step scores): **layer-split collapse — layer 3 drops below 50% needle selection at σ=0.60 (holds ≥50% to σ=0.55); layer 7 NEVER collapses (saturated positive scores, ~no KV reduction)** — the corridor's sparsity and its stress fragility are LAYER-3-DRIVEN; a per-layer threshold (σ₃ > σ₇) is follow-up material. Multi-key: the distractor block is selected at the SAME rate as the queried needle — no selection discrimination (attention weights still discriminate; decode 12/12).
- [x] **T3 — Gate wiring.** The Issue 584 Phase 3 promotion gate gains the Bench-814 needle row: the RELATIVE pin above (sparse-within-tolerance at σ=0.5). The ABSOLUTE SAS-class axis stays OPEN and is recorded as such: the 0.4B testbed's dense oracle never retrieves the needle (pw_rank top-1.3%, never top-1, at every rung 512→17322), so "pooled centroids destroy retrieval" is untestable on it — the absolute row needs a model whose dense oracle retrieves (Bonsai-27B on the 4090 via the GQA path, or any ≥1B with real long-context training). 64K rung priced at ~9h/arm (measured 10ns×n² — the MLA decode path's k_c/v_c re-up-projection, the known Phase-6 item); bench + cost model recorded in Bench 814 §Cost model.
- [-] **T4 — Centroid refinement (deferred behind T3).** Only if T3 shows a gap. Do NOT reach for the trained-selector class here — that is riir-train Plan 337's lane (Issue 560 recipe deltas); the modelless refinement must stay deterministic (max/top-k pooling, no GD). T1/T2 measured NO relative gap on this testbed; the per-layer threshold split (σ₃ > σ₇) from T2 is the first modelless candidate IF an absolute-axis gap ever shows.

## References

- Research 567 (parent note; SAS RULER limitation = the external evidence)
- Issue 584 (FlashMemory mechanism validation; Phase 1 Q4 = the accuracy check that did NOT cover needles)
- Bench 671 (1.8× decode at 64K on 4090 — the perf half of the corridor)
- riir-train Issue 560 / Plan 337 (the trained-selector lane — separate track, do not mix)
