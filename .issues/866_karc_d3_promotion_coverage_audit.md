# Issue 866 — KARC D3 split-config promotion: retrospective coverage audit + conditional `faer` spike

Status: **OPEN — retrospective audit issue of a LANDED default-on promotion (the decision is made; this issue audits its contract's coverage) + one conditional POC.**
Proposal: [Proposal 008 — KARC compute-unblock revisit](../.proposals/008_karc_compute_unblock_revisit.md)
Promotion record: [Bench 308 KARC GOAT](../.benchmarks/308_karc_goat.md) §Phase 5.3 / `katgpt-core/Cargo.toml` Phase 22 comment (2026-07-21, Issue 186 Path D3 split-config gate — `karc_forecaster` DEFAULT-ON)
Branch: `develop`

## Why this issue exists

Proposal 008's written next action ("wait for K=10, predicts 8.5 LT PASS") resolved WITHIN A DAY of the proposal, and in the opposite direction than its framing assumed: Bench 308 Phase 5.2 measured K=10 at 7.36/7.23 LT (plateau ~7.2–7.4 LT for M=8/R=2 once K ≥ 8; NRMSE passes at all three R=2 configs). Phase 5.3 then closed the last escape hatch (R=1 NRMSE hard floor ~4.79e-3). The compound gate (both G1 legs in ONE config) was declared structurally infeasible, **the gate was re-specified (Issue 186 Path D3, split-config), and `karc_forecaster` promoted to DEFAULT-ON on 2026-07-21** — verified in `Cargo.toml:863` (default list), the Phase 22 feature-def comment, and Bench 308's head + bottom-line records. A first draft of this issue wrongly framed Path D as an open deliberation; the verdict pass caught it.

**The residual question is narrower and audit-shaped:** the D3 contract's two passing legs were measured on **`ChebyshevBasis` at K=8** (NRMSE legs at M=8/R=2; threshold leg at M=24/R=1) on the **double-scroll attractor**. The deployed riir-engine consumer monomorphizations are different on every axis: the **default tier** is `HlaKarcForecaster` (`karc_bridge/mod.rs:85`) = `KarcForecaster<FourierBasis<HLA_KARC_M>, BELIEF_DIM, HLA_KARC_M, HLA_KARC_K>` — FourierBasis **M=8, K=4, R=1, d_h=256**, the tier every NPC runs (Lod1 needs no dispatch; `lod.rs:16,35` "always present, always Lod1"), with `Lod0Forecaster` (FourierBasis M=4/K=2) and `Lod2Forecaster` (FourierBasis M=8/K=8) reachable only after explicit tier promotion (`lod.rs:109`; dispatched via the 2-variant `KarcTieredForecaster` enum, `lod.rs:118`) — different basis family, M ≤ 8 (the value Bench 308 says plateaus below the threshold), on **HLA belief trajectories**, not the attractor corpus. riir-engine gates its runtime with its own six GOAT gates (`karc_runtime.md`: G1 2.71× … G6 1000×), but those are relative/exactness properties — per Plan 332, G1 is per-NPC personality **divergence** (a uniformly-wrong forecaster passes it with a larger margin), G2 is a curiosity-spike ratio, G5 is latency, G6 is chain commitment. Not one measures forecast accuracy against an absolute bar, so none can ratify NRMSE ≤ 1e-3 — the same relative-vs-absolute distinction Plan 306's own gate re-spec turned on. The question: whether the katgpt-rs **forecast-quality contract** was ever measured on any config a consumer actually constructs.

## Tasks

- [ ] T1 **Coverage audit of the D3 contract vs deployed configs — primary subject `HlaKarcForecaster` (the default tier), Lod0/Lod2 secondary.** (a) **Classify and record** what each of riir-engine's six `karc_runtime` GOAT gates asserts, against the explicit equivalence test: a downstream gate ratifies coverage only if it measures **the same quantity (forecast accuracy) against the same absolute bar (NRMSE ≤ 1e-3 / ≥ 8 LT)**. Current evidence says none does (divergence/ratio/latency/commitment gates) — (a) is expected to produce the QUALIFY record, not a RATIFY. (b) Run the cheap measurement: NRMSE + horizon at the **`HlaKarcForecaster` shape** (FourierBasis M=8/K=4/R=1, d_h=256) plus the Lod0/Lod2 shapes, on a belief-trajectory fixture, same protocol as Bench 308 Phase 5. (c) Verdict **RATIFY** (an absolute accuracy number already exists for a deployed shape — record where), **QUALIFY** (record the contract's scope limitation in Bench 308 + the feature-def comment; the T1(b) numbers become the deployed configs' own quality record), or **DEMOTE** (deployed-config quality is broken — file the demotion plan citing this issue). The one-page verdict record lands appended to this issue either way.
- [ ] T2 **Conditional `faer` spike (Path E) — ONLY on trigger.** Trigger: a SECOND heavy-BLAS consumer materializes in the workspace (the proposal's recorded condition; `katgpt-core` alone does not justify the dep-weight + CI-matrix cost). Then: 30-min integration spike behind opt-in `karc_faer_eig`, in-tree Householder+QL stays the no-dep fallback, pin `faer` to a minor version. Do NOT open this lane speculatively. (Context: `karc_householder_eig` already measured STAYS-OPT-IN — the direct Cholesky path is both faster and more accurate for G1-class work.)
- [ ] T3 **Record the verdict either way** in this issue + a one-line status update on Proposal 008; close + remove per the noise-reduction rule leaving the record in git history + the proposal status line. **Cross-repo corrections owed (landed in riir-ai with this issue's SHA cited — the cross-repo law: not landed until committed in the owning repo):** (1) `riir-ai/.plans/332_karc_runtime_npc_integration.md:34` states the deployed shape was "fixed at Plan 308 GOAT 'HLA-shaped config'" — post-D3 that claim is exactly what T1 tests, so the line needs a correction. (2) `riir-engine/src/karc_bridge/lod.rs:27-28` states the module "monomorphizes the three tiers into three concrete types and dispatches via a 3-variant `enum`" — contradicted by the 2-variant `KarcTieredForecaster` ninety lines below and by the module's own lines 16/35; a stale doc block in the exact file a T1 auditor opens to answer "which shapes ship?". Fold both one-line fixes into the same riir-ai commit.

## Honest caveats

1. **This audit does not re-litigate the D3 re-spec itself.** The compound gate's structural infeasibility is measured (R=2 × M=24 → d_h ≥ 166_752, Gram ≈ 222 GB; Phase 5.3's R=1 NRMSE floor). Re-arguing it is out of scope.
2. **K=12/K=14 and M=16+ are measured dead ends** — do not re-open them without a new mechanism.
3. **Two LOD surfaces exist** — katgpt-rs's `karc_lod_tier` (opt-in, G2 FAIL at 10k NPCs in riir-ai Bench 514) and riir-engine's `karc_bridge::lod` (deployed). T1 audits the DEPLOYED one; the opt-in primitive with a failing integration gate is outside the default-on contract's blast radius. Tier shapes MATCH across the two surfaces (verified: `Cargo.toml:1181` LOD0 D=8/M=4/K=2 d_h=64 · LOD1 D=8/M=8/K=4 d_h=256 · LOD2 D=8/M=8/K=8 d_h=512, R=1 throughout — identical to `lod.rs:11-13`); Lod1's absence from `KarcTieredForecaster` is the documented no-dispatch default, not drift.
4. The deliverable of T1 is a recorded RATIFY/QUALIFY/DEMOTE verdict with cited evidence — not code, and not a silent doc edit.
