# Plan 600 — FlashAR Confidence-Commit Upgrade: promote the DBTM κ∪floor anchor rule behind `flashar_anchor` (Issue 811 PoC winner)

**Status:** T1–T7 + T10 LANDED; **T8 + T9 GOAT gates MEASURED GREEN 2026-09-17** ([Bench 600](../.benchmarks/600_flashar_confidence_commit_goat.md)) — promotion to default **DEFERRED on corpus honesty** (acceptance requires real-text T9; the measured corpus is the lane's non-saturated pattern family); entry stays opt-in behind `flashar_anchor`, strided incumbent stays default. PoC evidence: [Issue 811](../.issues/811_confidence_commit_anchor_rule.md).

## Why (measured, one paragraph)

Issue 811's three-arm head-to-head on the trained mini-D2F (24 sequences, block=8, paired seeds, harness parity-pinned byte-identical to the production decode): the confidence rule extracts MORE anchors from the SAME Round-1 AR walk than the stride rule (8.0 vs 4.0 at κ=0.5), the DBTM floor empties the block 3.1–4.7× faster than the matched-threshold stride arm at κ ∈ {0.9, 0.99} (1.04–1.50 vs 3.25–7.00 fill steps at k=8; 134–194 µs vs 424–915 µs), quality is at parity everywhere (≤1.2σ on 192 samples; the best-quality cell is a confidence arm), and termination-within-k is property-proven at every (κ, k) cell including κ=0.999. The all-mask baseline (0.031–0.062 accuracy) confirms the anchor round carries the signal — selection rule quality is about how much of it survives the walk.

## Tasks

- [x] T1 PoC harness (three arms, one trained mini-D2F, paired rng) — Issue 811, shipped in the PoC commit
- [x] T2 arm table at NFE {1,2,4,8} × κ {0.5,0.9,0.99} — recorded in Issue 811
- [x] T3-partial pure math: `commit_time_star` (ignition_schedule) + `probability_order` (set_diffusion_schedule)
- [-] T3 Plan-381 sweep-bench arm (t\*-gated probability-ordered reveal vs uniform/ar/mdlm) — DEFERRED: `train_mini_set_causal`/`evaluate_set_causal_nelbo` take `&PositionOffsetSchedule` only; a probability-ordered arm needs a custom-order seam in the set-causal train/eval path. Seam issue FILED as [Issue 813](../.issues/813_set_causal_custom_order_seam.md) (2026-09-17, after T6 landed).
- [x] T4 verdict — PASS on the steps/termination axis at quality parity (the honest scope: quality is anchor-dominated on the toy and does not separate; no regression measured)
- [-] T5 UGC cross-check (confidence-greedy reveal KL vs the UGC certificate, Caveat #1) — DEFERRED: load-bearing only when the upgrade claims a QUALITY gain on real text; the PoC pass is on steps/termination at quality parity. Fold the KL measurement into this plan's G1 gate list before any default-promotion claim.
- [x] T6 `ConfidenceAnchorConfig` in `katgpt-forward::flashar_anchor`: `{ kappa: f32, floor: bool }` beside the existing `AnchorConfig` (stride stays the DEFAULT — demote-on-loss rule); selector fn (`select_confidence_anchors`) consumed by the new `anchor_then_fill_with` entry point, zero changes to the existing signature — LANDED 2026-09-16
- [x] T7 Round-1 walk change: `predict_anchors` records (argmax, max-softmax q) per position via optional out-params (`None` = incumbent, byte-identical — pinned by the parity test) — LANDED 2026-09-16
- [x] T8 Real-text GOAT gates before any default promotion: G1 quality on a non-saturated corpus (the toy is anchor-dominated — accuracy does not separate arms; use the eval set from the Plan-381/116 lane), G2 steps-to-converge + wall per block at matched quality, G3 no-regression on the strided incumbent (byte-identical with the config off), G4 alloc-free fill path (the candidate Vec is pre-allocated + reused) — MEASURED GREEN 2026-09-17, all four ([Bench 600](../.benchmarks/600_flashar_confidence_commit_goat.md)): G1 paired Δ ≥ 0 at every κ with SE ≤ 0.016 (the non-saturated corpus even SEPARATES arms — conf κ=0.5 nominally ahead +0.0137 ± 0.0083); G2 2.0–3.9× steps / 0.70–0.79× wall vs matched-stride; G3 parity tests green (no new pin needed); G4 5 allocs == 5 allocs across 4/8 rounds (`select_confidence_anchors_into` + `with_capacity(block_size)` landed with it). "Real-text" scope note: the corpus is the lane's pattern family made non-saturated — the strongest this repo's dllm lane builds today; see the promotion deferral below.
- [x] T9 UGC KL cross-check (Issue 811 T5) measured on the same corpus as T8-G1 — MEASURED GREEN 2026-09-17 ([Bench 600](../.benchmarks/600_flashar_confidence_commit_goat.md)): MC 8192×2 seeds per arm, realized KL(P_Z‖P̂) conf+floor 3.2518 vs matched-stride 3.2653 (ratio 0.996 — the greedy reveal distorts LESS than the incumbent); certificate bound 4Ĉ/8 = 5.673 as the random-order context; Caveat #1 empirically benign on this corpus
- [x] T10 Docs: `.docs/` entry + AGENTS feature-list line for the new config knob (opt-in; no default change without T8+T9 green) — DONE 2026-09-16: `.docs/02_inference/speculative_decoding.md` §Confidence-Commit Variant + `.docs/09_feature_catalog/opt_in_features.md` row + Cargo.toml feature comment

## Acceptance

Production seam lands opt-in under the existing `flashar_anchor` feature with the strided incumbent untouched (G3 byte-identical); promotion to default requires T8 all-green + T9 on real text — never on this toy's numbers alone.

**Resolution 2026-09-17:** T8 + T9 all green ([Bench 600](../.benchmarks/600_flashar_confidence_commit_goat.md)) but on the lane's non-saturated PATTERN corpus — the acceptance's "on real text" bar is not met and cannot be met until this repo grows a real-text D2F eval (text-trained D2F + token-level NLL/exact-match harness). **Not promoted;** `anchor_then_fill_with` + `ConfidenceAnchorConfig` stay opt-in behind `flashar_anchor`, the stride incumbent stays the `anchor_then_fill` default, and the demote-on-loss rule stands. The single remaining promotion precondition is that real-text eval.
