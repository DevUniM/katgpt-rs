# Plan 603 — Reflex Phase 1: engine + harness (riir-reflex skeleton + katgpt-rs contract)

**Status:** PLAN OPENED 2026-09-21 (owner "go" received in-session; [Proposal 014](../.proposals/014_katgpt_decision_engine_site.md) §Phase 1, caveats 2/4/5/6/7e/8 carried), commit `005037df5`. **T1.2 LANDED 2026-09-21** — `katgpt_core::decision_wire` (opt-in) + golden byte-pins + 11 module tests + the `katgpt-core:2074:decision_wire` test-gate row + README/catalog count bumps, all gates green (test_gate 6/6 rows, docs_gate 34/34); commit see git log. **T1.1 LANDED 2026-09-21 (owner delegated the repo birth to the session — "do remains, you are the owner"):** repo born at `/git/riir-reflex` (git init `develop`, private `gist-rs/riir-reflex` via gh, `develop` set as the GitHub default; birth commit `31207ec`, pushed) with BOUNDARY.md (domain test verbatim + the boundary-test passage + the Proposal-017 counter-case), AGENTS.md (house format), `.docs/sibling_layout.md` (the day-one dependency-graph artifact: ONE code-level dep + the primitive→substrate feature map), and the Cargo workspace skeleton (default `modelless` forwarding the six substrate features; `[[bin]]`+`[[test]]`+`[[bench]]` rows all carrying `required-features` in the birth commit — T1.1e). **Registration landed in the SAME session** — katgpt-rs `be2d90dd2` (repo_set.txt + repo count 21→22 + all 20 EVERY-scoped floor rows, measured at birth; repo_registration_gate 22/22 PASSED, agents_repo_set_gate PASSED, docs_gate 32/34 with the 2 reds being the Plan 605 sibling's UNCOMMITTED working-tree state: their engram_puct row owns the README count bump, their untracked .plans/605 owns the highwater bump) + riir-ai `927ab0d3f` (the CANONICAL none-row; contract run exit 0 at 22 repos / 318 edges, 0 violations / 0 rot). **T1.3 LANDED 2026-09-21** — the modelless engine (embed → pick_domain routing over unit centroids → corpus-is-the-model scoring → sigmoid+L1 → SigmoidGateCalibrator → fused score+distance abstain) + the std-only localhost HTTP edge + G2 (p99 49 µs ≤ 1 ms per 8-question decision set, tail support printed) + G4 (solve_into alloc-free under a canary-armed counting allocator; the wire boundary allocates by design and is reported-not-gated); measured birth geometry is recorded in the code (EMBED_DIM=256 — the 64-bucket first measurement washed the abstain geometry out to ±0.02 and is documented; gate slope 8 · (max_cos − 0.35) separates in-corpus ≈0.94 from off-corpus ≈0.06). **THE G4 SUBSTRATE HALF: `Lz4FlexDrafter::score_into`** landed UPSTREAM first (katgpt-rs `a577827c9` — the zero-alloc hot scorer: reusable compress table + output buffers on the drafter, parity-pinned vs `score`, gate-covered by the NEW `compression_drafter_alloc_gate` target + the PERF_ROWS grammar extended with an optional 4th features field + the `katgpt-core:2074:compression_drafter` lib row; test_gate PASS 8/8, canary FAILS as required). **T1.6 already VALIDATED** ([Bench 845](../.benchmarks/845_distance_abstain_goat.md)); the gate is WIRED as the engine's fused-abstain distance half. **T1.7 LANDED 2026-09-21** — the readout dispatch INHERITS Bench 817's verdict (narrow ≤8 = inverted label entropy, wide = argmax-label-prob), the dispatch table pinned by regression tests at both the module and integration boundaries; agreement-across-rereads is not a signal and is not implemented. **T1.4 PORT LANDED 2026-09-22 (riir-reflex `e956193`, pushed)** — the laya lane runs native-Rust, ONE atomic commit per the plan law: `laya` feature (opt-in: candle-core 0.11 CPU f32 + tokenizers 0.23 + sha2/blake3) + ModernBERT forward (both geometries, alternating global/sliding attention, per-type RoPE, layer-0 mlp_norm-only quirk) + the torch-semantics decision head (norm_first, **ReLU FF — torch's default activation; the pin doc's "GELU between" was wrong and is corrected in the same commit**) + `build_sequence` (Python-JSON render law, `preserve_order` serde_json — option insertion order IS the label order) + tokenizers-crate loading of the pinned BPE JSONs + runtime weight download (SHA-256 weights / blake3 small files; hub layout: english = repo root, `tokenizer/` + `encoder/config.json` subpaths → flat local names; dual-layout local reads so an HF snapshot root works via `LAYA_WEIGHTS_DIR`). **G5 PASSES per checkpoint, both dev and release profiles: 88/88 forwards, top-1 agreement 1.000 (gate ≥ 0.999), probability drift ≤ 3.1e-6 (gate ≤ 1e-3), act probabilities exact, conf drift ≤ 5.1e-5; structural parity EXACT (markers/seq_len/bucket/resolved temperature).** Measured traps recorded in the pin doc: candle's `Tensor::gelu()` is the TANH approximation (use `gelu_erf`), and the sdpa sliding-window radius is `local_attention // 2` — the attention module's `+1` is flash-path bookkeeping only (porting the +1 cost a 4-order drift hunt: length-correlated 2.9e-2 → 3e-6 after the fix). **Verdict round 2: AGREE** (the reviewer verified every fix mechanically — re-ran the guard, counted the lockfile, ran the router suite — and reproduced G5 independently on the pre-unification tree). Its three repair residuals closed at riir-reflex `2606d19`: the guard's final line now reads `PASSED (PARTIAL — skipped: …)` naming the unmeasured axis (the full_gate PARTIAL lesson), the G5 layer runs the 27 laya-gated lib tests alongside the parity target (they were compiled by layer 5 and executed by nothing), and the layer numbering is consistent [n/7]. Full guard verified end-to-end with weights armed: 7/7 layers, parity 2/2 at release on the UNIFIED tokenizers (32.8 s), lib suite 27/27. **Verdict-review closures landed the same day (riir-reflex `1ef3e8e` + issue closure `8e58cc5`):** the Rust script-detector Router ported (`lang.rs` + `router.rs`, the reference's test_router.py behavioral suite as passing unit tests; LRU loading deliberately not ported — the harness holds agents, the router stays pure decision logic per the plan's scope); tokenizers UNIFIED to candle-core's own 0.22.2 (two copies of a heavy crate → one; G5 RE-RUN on the unified version, 88/88, drift ≤ 3e-6); BOUNDARY.md `## May depend on` rows for tokenizers/sha2/blake3/serde (`.issues/001`, filed-then-fixed in the same commit — the workspace guard's C3 checks cross-repo PATH deps only and is structurally blind to crates.io deps, which is how four landed without rows); the local guard gained the laya lint layer + the G5 parity layer (SKIP-LOUD without a weights root); the render.rs dead conditional removed. **T1.5 LANDED 2026-09-22 (riir-reflex `26ec6c6` harness runner + `0ee7d3e` Bench 001 record; full entry on the T1.5 checkbox):** 9 suites × 2 lanes over byte-identical questions, metrics verbatim from `.docs/laya_bench_protocols.md` §5 (the committed port spec), CI-regenerated tables + dispatch-only workflow; the port REPRODUCES the reference's published numbers within noise (ag_news 0.9500 vs 0.953, emotion 0.5925 vs 0.600, typed 0.7445 vs 0.766, base 0.3575/0.3490 vs "~0.36"); G1 calibration PASS 8/9 vs the conformal-naive floor; three measured lessons on the record (birth abstain thresholds do not transfer → per-suite cal-slice 30th-percentile fit; cal-slice-vs-own-corpus self-inclusion; the always-wrong collapse reads ECE 0.000 through the left-open bins). **T1.8 CLOSED same day (catalog note in this repo + riir-reflex README/AGENTS + the banking77 manifest resolution) — Phase 1 COMPLETE, all acceptance rows green; the `structured_reads` promotion line correctly NOT pulled (the engine never consumes it).**

## Why (one paragraph)

Phase 1 is the primary deliverable and needs no marketing call: it alone discharges `structured_reads`' recorded promotion trigger ("when a consumer appears" — verified: no consumer exists today) and builds the Issue-125 corpus engine with standalone value. The engine serves typed decisions over the LANDED substrate (modelless heads, `SigmoidGateCalibrator`-calibrated bridges, `structured_read`, `rule_embed` vessel, `Lz4FlexDrafter` corpus-is-the-model scoring), benched head-to-head against the Laya family in four lanes (byte-identical questions). Standalone value holds even if Phases 2–4 never ship. Boundaries: new private repo `riir-reflex` (decision-engine serving + comparison + contribution; NOT game runtime, NOT code healing) — the new-repo decision passes the boundary test and cites the Proposal-017 counter-case; zero game deps, zero Python deps (owner directive — the laya lane is the native-Rust port, cost accepted).

## Tasks

- [x] T1.1 **riir-reflex repo creation + workspace registration (one dedicated commit, boundary gates green before AND after). LANDED 2026-09-21 — owner delegated the birth to the session.** (a) repo born at `/git/riir-reflex`, git init `develop`, `gh repo create gist-rs/riir-reflex --private`, `develop` set default, pushed; (b) root BOUNDARY.md with the domain test verbatim + the boundary-test passage + the Proposal-017 counter-case; (c) `.docs/sibling_layout.md` (ONE code-level dep + feature map + deferred arms); (d) the registration: katgpt-rs `be2d90dd2` (repo_set.txt + AGENTS.md 21→22 + 20 measured floor rows; registration + repo-set gates PASSED) + riir-ai `927ab0d3f` (CANONICAL none-row; contract run 22 repos / 318 edges, 0 violations / 0 rot); (e) repo-birth gate discipline held in the birth commit — every gated target ([[bin]] + [[test]] engine_gates + [[bench]] decision_set_goat) carries `required-features = ["modelless"]`; (f) cargo green at default + `--all-features` + `--no-default-features` (clippy -D warnings ×3 postures, tests, bench).
- [x] T1.2 **katgpt-rs wire-contract types (+ feature flag + GOAT scaffold). LANDED 2026-09-21** — `katgpt_core::decision_wire` (opt-in `decision_wire`): `DecisionRequest { state, questions }` → `DecisionResponse { answers, routing, calibration }` over the Jev/laya vocabulary (`choice` with request-time options + criteria / `score` ordinal rubric / `noul` yes/no — Research 562/574/576 vocabulary verbatim); answers carry outcome + probabilities + confidence; **abstention is a first-class answer** (`outcome: None` — Jev cannot abstain, the recorded Research-562 flaw; distribution still rides for risk–coverage); `Calibration` is the G1 Report-the-Floor surface; `Routing` names the lane + reason. Fields ALWAYS serialized — postcard is positional (the ptg_functor_edges law measured live: skip_serializing_if broke the round-trip with `DeserializeUnexpectedEnd`); golden JSON byte-pins + serde_json + postcard round-trips + fail-closed `WireError` validation (arity/ranges/finiteness/id-alignment/kind-agreement), 11 module tests. Gate-covered by the NEW test-gate row `katgpt-core:2074:decision_wire` (measured; the tests are invisible at default features — the green-zero trap). README 629→630 ×4 sites + examples/README ×1 + `.docs/09_feature_catalog` entry — count-prose in the SAME commit. Zero new deps (serde/postcard already non-optional). Validated: clippy clean at default + feature postures, test_gate PASS 6/6 rows, docs_gate PASS 34/34.
- [x] T1.3 **engine modelless lane behind localhost HTTP (in riir-reflex; landed primitives only). LANDED 2026-09-21** — routing head = `pick_domain` over per-domain unit centroids (the KernelExpertRouter math; `CalibratedActionBridge`'s fixed-`A` wrapper cannot select over request-time option spaces, so its SUBSTANCE — the `SigmoidGateCalibrator` — is wired on the answer confidence and the bridge stays forwarded for fixed-A lanes; decision recorded in the engine docs); option scoring = `Lz4FlexDrafter::score_into`, the zero-alloc hot scorer landed upstream for this lane (katgpt-rs `a577827c9`, alloc-gate target + PERF_ROWS features-field grammar + lib row); probabilities = per-option sigmoid at τ then L1 (sigmoid, never softmax); abstain = the fused score+distance gate with thresholds from the birth MEASUREMENT (in-corpus ≈0.94 / off-corpus ≈0.06; EMBED_DIM 256 — the 64-bucket wash-out is documented in the const doc); serving = ONE std-only binary, loopback default, /decide + /feedback + /healthz, no daemon framework; G2 p99 49 µs ≤ 1 ms per 8-question set (tail support printed) + G4 solve_into alloc-free under a canary-armed counting allocator land WITH the lane in `benches/decision_set_goat.rs` (cargo bench = release by construction); the HTTP edge contract is gate-tested over a real loopback socket; hot-loop rules hold (zero-alloc core + caller-owned scratch; wire materialization is the allocating boundary, reported not gated). The `rule_embed` vessel arm is deferred to the harness phase (the frozen-artifact precedent needs a consuming lane first) — recorded in sibling_layout.
- [x] T1.4 **laya lane: NATIVE-RUST runner (no Python anywhere — owner directive). LANDED 2026-09-22, riir-reflex `e956193` + verdict-review closures — see the status line above for the G5 numbers + measured traps; the full port record lives in `riir-reflex/.docs/laya_reference_pin.md`. The Rust script-detector Router is PORTED the same day (`lang.rs` + `router.rs`, the reference's test_router.py behavioral suite as unit tests, all passing; LRU loading machinery deliberately not ported — the harness holds agents).** candle or in-crate forward over their Apache-2.0 safetensors UNMODIFIED: ModernBERT-large (421M) + mmBERT-base (322M, RoPE→8k) + the mask-scoring head + their tokenizers (~~WordPiece-class~~ **BPE — corrected by the 2026-09-22 pin; the `tokenizers` crate loads both JSONs directly**, the mmBERT 256k vocab is the bigger half) + a Rust script-detector Router (pure logic). Their protocol mirrored: byte-identical questions, fixed seed, all three checkpoints. **G5 parity gate BEFORE any published number: top-1 agreement ≥ 99.9% + probability drift ≤ 1e-3 vs the reference checkpoint, per checkpoint, on a fixed fixture corpus — bit-identity NOT claimed (op order differs from PyTorch).** The parity test ships as a `[[test]]` row with `required-features = ["laya"]` in the SAME commit (the repo's highest-stakes silent-now candidate — a whole-file `#![cfg(feature = "laya")]` target without its row prints `ok. 0 passed`, exit 0 forever, and the arena publishes on it); the row is proven at the exact laya feature set. **Precondition DONE 2026-09-22 (slice 1, riir-reflex `d617724`): the laya sources are pinned at full sha `573e5b62696ba441230cd6be71d593331b5d23af` (`.raw/laya` clone) and the checkpoints by full-file SHA-256 + 12 BLAKE3-pinned small files — canonical record `riir-reflex/.docs/laya_reference_pin.md`** (encoder + head geometry, tensor inventory from safetensors headers, the inference contract incl. the temperature clamp that rewrites the shipped `choice:11+` bucket, and the softmax-stays parity law); Research 576's live-fetch caveat is discharged. **Slice 2 DONE same day (riir-reflex `4f77583`): the G5 fixture corpus `tests/fixtures/laya_parity_v1.jsonl`** (BLAKE3 `f2a00354…`, 36 rows / 37 questions / 98 forwards — every temperature bucket incl. the clamped `choice:11+`, the k=1 forward, Python-JSON criterion renders, mask-literal neutralization, both truncation windows, ten scripts + the thai-collapse row). **Slice 3 DONE same day (riir-reflex `80a9e6c`): the reference capture `tests/fixtures/laya_parity_expected_v1.json`** (BLAKE3 `4f4dcc37…`, 88 forwards via the PINNED laya code imported from `.raw/laya`, agent.py-verbatim load: fp32 weights over F16 storage, `reference_compile=False`, eval, CPU, no autocast; in-capture verification: clamped 0.5 on `choice:11+`, per-checkpoint `score:2` fallback divergence 1.2514 vs 1.0374, exact 512/1024 truncation, thai collapse conf 0.0002). Capture env reusable at `/tmp/laya-ref-venv` (torch 2.14.0 + transformers 5.17.0 + HF cache with all three weights). REMAINING = the port itself, ONE atomic commit: `laya` feature + candle dep + ModernBERT forward (both geometries, alternating window attention) + head + `build_sequence` (Python-JSON render law) + tokenizers-crate loading + runtime weight download (SHA-256 pins) + the G5 parity `[[test]]` row reading both fixture files. Weights runtime-downloaded from HF with the SHA-256-pinned per-file digests — never bundled (the Phase-2 lean-assets rule lands with the download path). GPU note: parity fixtures run on CPU/4090 via issue for long runs; M3 first.
- [x] T1.5 **harness: laya's public tasks + our fixtures; Report-the-Floor metrics; CI-regenerated tables. LANDED 2026-09-22 (riir-reflex `26ec6c6` + `0ee7d3e`, record `riir-reflex/.benchmarks/001_phase1_harness.md`, tables `.benchmarks/001_phase1_tables/`, CI lane `.github/workflows/harness_tables.yml` dispatch-only).** 9 suites × 2 lanes over byte-identical questions: typed_decisions (ALL 400 rows / 2000 q) + ag_news/emotion (400) + sst5 (600) + prompt_injections (ALL 116) + xnli_en (300) + massive_intent_en (300, universe 59 from the FULL split) + banking77 (500, universe 77, `mteb/banking77` mirror — PolyAI is script-based, unservable) + code_fixtures (16 generated cases from this repo's own fn spans, gold programmatic). Metrics verbatim from the protocols spec §5 (hard/ECE-15/Brier/NLL/AURC/soft/score); G1 = raw-vs-calibrated readout ECE vs the conformal-naive floor. **Results: laya[typed] typed_decisions 0.7445 (published 0.766), base ckpts 0.3575/0.3490 (published "~0.36"), ag_news 0.9500 (published 0.953), emotion 0.5925 (published 0.600) — the port reproduces the reference within noise; modelless near-chance on OOD text (structural, caveat 4) with G1 PASS 8/9 (massive FAIL recorded — non-monotone routing signal Platt cannot repair). Datasets: HF datasets-server `/rows` JSON (`scripts/fetch_datasets.sh`, blake3-digested `.docs/dataset_manifest.md`).** Three measured lessons the first runs paid for: (a) the fused-gate birth thresholds (0.35/0.5) do NOT transfer — 64–100% abstain on real-corpus suites — so the harness FITS both thresholds per suite at the cal-slice 30th percentile (the T1.6 arena posture ρ=30%); (b) the calibration slice must not sit inside its own reference corpora (cal case scoring cos 1.0 against ITSELF inflated every cal quantile — the corpus pool now starts AFTER the cal slice); (c) a calibrator that learns "always wrong" collapses confidence to exactly 0.0 and abstains everything — correct autonomous behavior, and the reason some rows read readout-ECE 0.000 (the protocol's ECE bins are left-open `(0,1]`, so zero-confidence rows fall in NO bin — read those as n/a, never as perfect).
- [x] T1.6 **corpus-distance abstain arm** — VALIDATED 2026-09-21 ([Bench 845](../.benchmarks/845_distance_abstain_goat.md), GO for the opt-in lane; `distance_abstain` feature + `CorpusDistanceGate`): beats the score-threshold ABSTAIN baseline in the arena's target world (W1 OOD-blind: AURC −17%, Δ sel-acc +0.97 pp @ ρ=30%, 8/8 replicates, ~89% of the labeled oracle's gain) with the W2 negative control proving the OOD mechanism. ⚠ Rank fusion is load-bearing (raw min(p, d_conf) degenerates to distance-only). NOT promoted to default — margin_gate precedent: the live consumer is THIS plan's engine, where it joins as the abstain arm behind outcome calibration.
- [x] T1.7 **confidence readout policy (inherit, don't re-derive — caveat 6). LANDED 2026-09-21** — `src/readout.rs`: inverted normalized label entropy for option counts ≤ `NARROW_MAX_OPTIONS` (8), argmax-label-prob above; the dispatch TABLE pinned by regression tests at the module boundary (the K=8/K=9 boundary arm) and cross-checked at the integration boundary; agreement-across-rereads NOT implemented (Bench 817's verdict). The constant is repo-owned operating policy — changing it re-pins the test in the same commit, never re-derives the bench.
- [x] T1.8 **docs + registration closure. LANDED 2026-09-22** — registration DONE at birth (katgpt-rs `be2d90dd2` + riir-ai `927ab0d3f`); riir-reflex AGENTS.md + README (results + honest reading) landed with the harness; the katgpt-rs `decision_wire` catalog note now carries the Phase-1-consumer paragraph (this commit); the `.docs/dataset_manifest.md` banking77 gap resolved via the mteb mirror. The `structured_reads` root promotion line is correctly NOT pulled: the engine consumes the drafter/routing/calibration substrate, never `structured_read` (the recorded re-arm trigger stays armed — one line when a consumer appears).

## GOAT gate (binds every promotion in this plan)

- **G1 (calibration):** engine decision-level ECE/Brier beats (a) its own uncalibrated outputs and (b) the conformal-naive floor — Report-the-Floor; a lane claiming "calibrated" without flooring fails.
- **G2 (perf):** modelless lane p99 ≤ 1 ms per decision set in-process; `structured_read` ≤ 0.5× full-loop (Bench 816's bar).
- **G3 (no regression):** riir-reflex consumes katgpt-rs, never edits it; healer score-bench + game benches untouched; katgpt-rs additions count-pinned in the existing gates.
- **G4 (alloc):** modelless hot path alloc-free (bench-811 counting-allocator convention), canary-armed.
- **G5 (laya-port parity):** top-1 ≥ 99.9% + p-drift ≤ 1e-3 vs reference, per checkpoint, on the fixture corpus — before ANY published table cites its numbers; a failed parity gate marks the lane PROVISIONAL in every table it appears in.
- **Arena integrity:** tables CI-regenerated; the flywheel's specialist retrain fires ONLY at the measured corpus threshold and is GOAT-gated vs the modelless default (Bench-062 discipline), freeze/thaw-committed, demote-the-loser on loss — Phase 3 territory, named here so no Phase-1 shortcut pre-arms it.

## Benchmarks to mind (created in this phase)

- G2/G4 bench binary in riir-reflex (decision-set latency + alloc counting, release profile — a debug-profile latency gate measures an unoptimised binary).
- G5 parity fixtures per checkpoint (pinned inputs, reference logits captured once at pin time, drift asserted ≤ 1e-3).
- Harness tables land under riir-reflex `.benchmarks/` (numbering discipline applies from repo birth: `.highwater` per directory).

## Caveats carried from the proposal (READ BEFORE IMPLEMENTING)

1. **No Python anywhere in the lane** (owner directive): no sidecar, no `uv`, no HF transformers dependency — candle/in-crate forward only. "No Python required" is also the product story.
2. **Trademark/nominative care:** "Jev" is TypeSafe's product name — nominative use + the laya-style disclaimer only; no brainfunctioncollapse prose/games/benchmark text copied; protocols restated with citation.
3. **The 20 Hz / determinism claims are modelless-lane-only** — per-lane scoping in every table, or the comparison is a false claim by aggregation.
4. **Zero-shot breadth loss is structural today** — the arena publishes per-task honesty INCLUDING the losses; that is the adopted posture, not a risk to mitigate away.
5. **The hosted plane is OUT of this plan** — Phase 4 sketch; Phases 1–3 touch no money; no owner constants are exercised here.
6. **Cross-repo law:** not landed until committed in the owning repo with the SHA cited — T1.1's registration spans katgpt-rs + riir-ai + siblings and is one dedicated commit in each touched repo, never a worktree-only edit.

## Acceptance

Phase 1 is DONE when: riir-reflex exists with green default + all-features builds and its gates wired (T1.1); the wire contract is count-pinned in katgpt-rs (T1.2); the modelless lane serves decisions behind localhost HTTP at G2+G4 green (T1.3); the laya lane runs native-Rust with G5 parity measured per checkpoint (T1.4); the harness emits CI-regenerated honest tables over both lanes (T1.5); the abstain arm + readout policy are wired (T1.6+T1.7); docs + registration are closed (T1.8). Phase 2 (the site + distribution, GREEN-LIT) is a separate plan; the flywheel (Phase 3) needs only T1.3.
