# Plan 603 — Reflex Phase 1: engine + harness (riir-reflex skeleton + katgpt-rs contract)

**Status:** PLAN OPENED 2026-09-21 (owner "go" received in-session; [Proposal 014](../.proposals/014_katgpt_decision_engine_site.md) §Phase 1, caveats 2/4/5/6/7e/8 carried), commit `005037df5`. **T1.1 BLOCKED on repo birth** — the repo directory (`/Users/katopz/git/riir-reflex`) is outside this session's sanctioned write scope and the `gist-rs` GitHub repo is an owner op; `repo_set.txt` is DERIVED (BOUNDARY.md + `.git` walk) and must never be hand-edited, so the 22-file registration cannot precede the directory. Owner unblock: `mkdir /Users/katopz/git/riir-reflex` (+ optionally `gh repo create gist-rs/riir-reflex --private`) — then T1.1 executes whole. **T1.2 is the unblocked next execution.** T1.6 already VALIDATED ([Bench 845](../.benchmarks/845_corpus_distance_abstain_goat.md)). Everything else unchecked.

## Why (one paragraph)

Phase 1 is the primary deliverable and needs no marketing call: it alone discharges `structured_reads`' recorded promotion trigger ("when a consumer appears" — verified: no consumer exists today) and builds the Issue-125 corpus engine with standalone value. The engine serves typed decisions over the LANDED substrate (modelless heads, `SigmoidGateCalibrator`-calibrated bridges, `structured_read`, `rule_embed` vessel, `Lz4FlexDrafter` corpus-is-the-model scoring), benched head-to-head against the Laya family in four lanes (byte-identical questions). Standalone value holds even if Phases 2–4 never ship. Boundaries: new private repo `riir-reflex` (decision-engine serving + comparison + contribution; NOT game runtime, NOT code healing) — the new-repo decision passes the boundary test and cites the Proposal-017 counter-case; zero game deps, zero Python deps (owner directive — the laya lane is the native-Rust port, cost accepted).

## Tasks

- [-] T1.1 **riir-reflex repo creation + workspace registration (one dedicated commit, boundary gates green before AND after).** DEFERRED-BLOCKED: the repo directory is outside this session's write scope and the `gist-rs` GitHub repo is an owner op; `repo_set.txt` is derived (BOUNDARY.md + `.git` walk, drift-fail-gated) so the registration cannot precede the directory. Owner unblock: `mkdir /Users/katopz/git/riir-reflex` (+ optional `gh repo create gist-rs/riir-reflex --private`); everything below then executes as written. (a) Create `/git/riir-reflex` (git init, `develop` default, private under `gist-rs`); (b) root `BOUNDARY.md` with the domain test verbatim ("decision-engine serving + comparison + contribution; NOT game runtime, NOT code healing") + the boundary-test passage (borrowed patterns reimplementable ~200 LOC + zero game-domain coupling from ANY feature combination → new repo; Proposal 017's `riir-games` extraction REFUSED for bidirectional engine coupling — the counter-case that decides the other branch); (c) the day-one dependency-graph artifact (`sibling_layout.md` shape: default build = ONE code-level dep `katgpt-core`; opt-in feature arms consume non-game whole substrates only where reimplementation would be absurd duplication — the Plan-005 nuance; no game crate reachable from ANY feature combination); (d) the 22-file workspace registration in every consuming repo (`repo_set.txt` + boundary gates + floors — registering a contract repo is a 22-file operation, measured; expect ~19 sweep-pin reds until pinned, the Issue-793 class, all in the registration commit); (e) repo-birth gate discipline: every `#![cfg]`-gated test target gets a `[[test]]` row with `required-features` in the SAME commit, proven at the EXACT feature set (`--no-run` names the executable, then one run for the passed count — the silent-now rule); (f) Cargo workspace skeleton (root crate + the feature list below, `cargo check` green at default + `--all-features`).
- [ ] T1.2 **katgpt-rs wire-contract types (+ feature flag + GOAT scaffold).** `DecisionRequest { state, questions }` → `DecisionResponse { answers, routing, calibration }` in Jev/laya-compatible vocabulary (`choice`/`score`/`noul`); small public crate or `katgpt-core::types` behind `decision_wire` (opt-in; promotion is the GOAT path); serde round-trip + golden fixtures count-pinned in the existing gates (docs gate + test gate floors move in the same commit as the types); README feature-count claim sites bumped (the 627→628 twin-discipline lesson is the SAME commit rule for count-prose).
- [ ] T1.3 **engine modelless lane behind localhost HTTP (in riir-reflex; landed primitives only).** pick_domain / `CalibratedActionBridge` + rule_embed vessel (`rule_embed_frozen_v1.bin` precedent) + corpus-is-the-model scoring via katgpt-rs's `Lz4FlexDrafter` (pattern lineage — the riir-games-quest wrapper is NEVER a dep). Serving shape: localhost-only bind by default, one binary, no daemon framework; the modelless hot path is ns–µs tier — hot-loop rules apply (no alloc per decision, fixed-size option arrays, scratch buffers passed in, `papaya` only if shared maps appear); HTTP layer is cold-path (hexagonal at the edge only). Bench: G2 (p99 ≤ 1 ms per decision set in-process) + G4 (alloc-free, the bench-811 counting-allocator convention, canary-armed) land WITH the lane, not after.
- [ ] T1.4 **laya lane: NATIVE-RUST runner (no Python anywhere — owner directive; multi-week long pole, accepted).** candle or in-crate forward over their Apache-2.0 safetensors UNMODIFIED: ModernBERT-large (421M) + mmBERT-base (322M, RoPE→8k) + the mask-scoring head + their tokenizers (WordPiece-class; the mmBERT 256k vocab is the bigger half) + a Rust script-detector Router (pure logic). Their protocol mirrored: byte-identical questions, fixed seed, all three checkpoints. **G5 parity gate BEFORE any published number: top-1 agreement ≥ 99.9% + probability drift ≤ 1e-3 vs the reference checkpoint, per checkpoint, on a fixed fixture corpus — bit-identity NOT claimed (op order differs from PyTorch).** The parity test ships as a `[[test]]` row with `required-features = ["laya"]` in the SAME commit (the repo's highest-stakes silent-now candidate — a whole-file `#![cfg(feature = "laya")]` target without its row prints `ok. 0 passed`, exit 0 forever, and the arena publishes on it); the row is proven at the exact laya feature set. **Precondition: re-pin the laya sources at a full sha (`.raw/` clone) before ANY published table is CI-generated against their numbers** (Research 576's live-fetch provenance caveat is not sufficient for landed artifacts). Weights runtime-downloaded from HF with BLAKE3-pinned per-file digests — never bundled (the Phase-2 lean-assets rule lands with the download path). GPU note: parity fixtures run on CPU/4090 via issue for long runs; M3 first.
- [ ] T1.5 **harness: laya's public tasks + our fixtures; Report-the-Floor metrics; CI-regenerated tables.** Tasks: laya's typed-decisions (400 cases / 2,000 decisions), AG News, DAIR Emotion, Banking77, SST-5, prompt-injections, MASSIVE/XNLI subsets + our code-domain fixtures (healer retrieval_eval spans, fixseq-ring verdict shapes). Metrics per lane: top-1, soft accuracy, Brier, **decision-level ECE measured against the conformal-naive floor** (Research 322 / Plan 340 — a lane claiming "calibrated" without beating the floor is marked, not trusted), p50/p99 latency, abstain rate + selective accuracy, determinism (bit-identical repeat runs — modelless lane only, caveat 5 scoping), cost. Honest per-task tables INCLUDING the losses (adopted posture — the same crowd that audited TypeSafe audits us); **published tables are CI-regenerated from the harness — a hand-typed number on the site is a defect by definition**; the CI-table lane lands as a repo workflow in this phase so Phase 2's site only LINKS it.
- [x] T1.6 **corpus-distance abstain arm** — VALIDATED 2026-09-21 ([Bench 845](../.benchmarks/845_corpus_distance_abstain_goat.md), GO for the opt-in lane; `distance_abstain` feature + `CorpusDistanceGate`): beats the score-threshold ABSTAIN baseline in the arena's target world (W1 OOD-blind: AURC −17%, Δ sel-acc +0.97 pp @ ρ=30%, 8/8 replicates, ~89% of the labeled oracle's gain) with the W2 negative control proving the OOD mechanism. ⚠ Rank fusion is load-bearing (raw min(p, d_conf) degenerates to distance-only). NOT promoted to default — margin_gate precedent: the live consumer is THIS plan's engine, where it joins as the abstain arm behind outcome calibration.
- [ ] T1.7 **confidence readout policy (inherit, don't re-derive — caveat 6).** Bench 817's verdict binds: label-entropy for narrow option sets, argmax-label-prob for wide ones; agreement-across-rereads is NOT a better ranking signal on deterministic forwards. The engine's readout dispatch ships with a regression test pinning the dispatch table, not a fresh derivation.
- [ ] T1.8 **docs + registration closure:** BOUNDARY.md drift ledger seeded empty; riir-reflex AGENTS.md (sibling layout, build commands, lint-heal house rule, numbering discipline — the house format); katgpt-rs `.docs/` gets the Phase-1 landing entry + the `structured_reads` root promotion line lands THE SAME COMMIT as the first real consumer (one line, the recorded re-arm trigger — do not pull it forward).

## GOAT gate (binds every promotion in this plan)

- **G1 (calibration):** engine decision-level ECE/Brier beats (a) its own uncalibrated outputs and (b) the conformal-naive floor — Report-the-Floor; a lane claiming "calibrated" without flooring fails.
- **G2 (perf):** modelless lane p99 ≤ 1 ms per decision set in-process; `structured_read` ≤ 0.5× full-loop (Bench 816's bar).
- **G3 (no regression):** riir-reflex consumes katgpt-rs, never edits it; healer score-bench + game benches untouched; katgpt-rs additions count-pinned in the existing gates.
- **G4 (alloc):** modelless hot path alloc-free (bench-811 counting-allocator convention), canary-armed.
- **G5 (laya-port parity):** top-1 ≥ 99.9% + p-drift ≤ 1e-3 vs reference, per checkpoint, on the fixture corpus — before ANY published table cites its numbers; a failed parity gate marks the lane PROVISIONAL in every table it appears in.
- **Arena integrity:** tables CI-regenerated; the flywheel's specialist retrain fires ONLY at the measured corpus threshold and is GOAT-gated vs the modelless default (Bench-062 discipline), freeze/thaw-committed, demote-the-loser on loss — Phase 3 territory, named here so no Phase-1 shortcut pre-arms it.

## Benchmarks to mind (created in this phase)

- G2/G4 bench binary in riir-reflex (decision-set latency + alloc counting, release profile — a debug-profile latency gate measures an unoptimised binary).
- G5 parity fixtures per checkpoint (pinned inputs, reference logits captured once at pin time, drift asserted ≤ 1e-3).
- Harness tables land under riir-reflex `.benchmarks/` (numbering discipline applies from repo birth: `.highwater` per directory).

## Caveats carried from the proposal (READ BEFORE IMPLEMENTING)

1. **No Python anywhere in the lane** (owner directive): no sidecar, no `uv`, no HF transformers dependency — candle/in-crate forward only. "No Python required" is also the product story.
2. **Trademark/nominative care:** "Jev" is TypeSafe's product name — nominative use + the laya-style disclaimer only; no brainfunctioncollapse prose/games/benchmark text copied; protocols restated with citation.
3. **The 20 Hz / determinism claims are modelless-lane-only** — per-lane scoping in every table, or the comparison is a false claim by aggregation.
4. **Zero-shot breadth loss is structural today** — the arena publishes per-task honesty INCLUDING the losses; that is the adopted posture, not a risk to mitigate away.
5. **The hosted plane is OUT of this plan** — Phase 4 sketch; Phases 1–3 touch no money; no owner constants are exercised here.
6. **Cross-repo law:** not landed until committed in the owning repo with the SHA cited — T1.1's registration spans katgpt-rs + riir-ai + siblings and is one dedicated commit in each touched repo, never a worktree-only edit.

## Acceptance

Phase 1 is DONE when: riir-reflex exists with green default + all-features builds and its gates wired (T1.1); the wire contract is count-pinned in katgpt-rs (T1.2); the modelless lane serves decisions behind localhost HTTP at G2+G4 green (T1.3); the laya lane runs native-Rust with G5 parity measured per checkpoint (T1.4); the harness emits CI-regenerated honest tables over both lanes (T1.5); the abstain arm + readout policy are wired (T1.6+T1.7); docs + registration are closed (T1.8). Phase 2 (the site + distribution, GREEN-LIT) is a separate plan; the flywheel (Phase 3) needs only T1.3.
