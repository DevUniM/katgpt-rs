# Plan 606 — Reflex Phase 2: binary distribution + arena site (Proposal 014 Phase 2)

**Status:** COMPLETE 2026-09-22 — every task `[x]`. Site live: <https://reflex.gist.rs> (custom domain owner-attached 2026-09-22; workers.dev still answers); release: `gist-rs/reflex` v0.1.1 — the FULL five-target cargo-heal matrix (macOS aarch64+x86_64 · linux musl x86_64+aarch64 · windows-gnu), scoop manifest landed WITH the windows asset.

Proposal: [`.proposals/014_katgpt_decision_engine_site.md`](../.proposals/014_katgpt_decision_engine_site.md) (Phase 2, owner GREEN-LIT 2026-09-21). Phase 1 record: `riir-reflex` Bench 001 + `603` (ALL `[x]`).

Shape: the cargo-heal Plan-105 pattern (binary-only public releases, source stays private in `riir-reflex`) + the static arena site at `reflex.gist.rs` (dark-red rust theme, `#B7410E`/`#7C1D05`; public product name **Reflex**). Deploy posture: **manual** (owner call — GitHub free-tier limit; no Actions-dependent publish lane this phase; local builds + `gh release create`).

- [x] T1.1 `src/build_stamp.rs` in riir-reflex — `--version`/`-V` prints the COMPILED feature set (build.rs reads cargo's `CARGO_FEATURE_*`; cannot drift from a hand-typed list) + `release set: STALE — missing …` + the exact rebuild command when the shipped set is incomplete (the cargo-heal Issue-117 shape). `RELEASE_FEATURES = modelless+laya` is the one typed constant. (`94f6a91` — `e56744b`.)
- [x] T1.2 serve.rs CORS seam — `RIIR_REFLEX_ALLOWED_ORIGIN` (comma list) gates an OPTIONS preflight (204 + ACAO + allow-methods/headers) and ACAO echo on listed origins only; default OFF (no ACAO anywhere) keeps the drive-by posture closed; `Vary: Origin`. The playground prints the exact launch command. Tests both directions. (`tests/serve_cors.rs`, 7 arms; `serve_listener_with` = the explicit allow-list seam, no env mutation across test threads.)
- [x] T1.3 bin CLI — bare invocation = serve (the just-works posture unchanged); `--version`/`-V` → the build stamp; unknown flags refuse exit 2 naming the stamp facts.
- [x] T1.4 `[profile.dist]` (strip + lto fat + codegen-units 1) — benches keep plain `release` (the cargo-heal profile lesson); manual release builds use `--profile dist`.
- [x] T1.5 `scripts/binary_leak_scan.sh` (the Plan-105 T2.5 port) + green on local dist builds of BOTH macOS targets (strip + `--remap-path-prefix $HOME=/build`).
- [x] T1.6 `scripts/build-release.sh` — one command per target: dist-profile build (strip + remap) → package tar.gz (binary + `THIRD_PARTY_LICENSES.md` at the archive ROOT) → SHA256SUMS. Licenses via cargo-about (`.github/about/`, 1,132-line doc generated clean — the laya tree introduced no unaccepted license).
- [x] T1.7 laya-in-release posture verified — weights NOT in the archive (runtime HF download, SHA-256 pins, `LAYA_HOME` cache); Apache-2.0 attribution row in THIRD_PARTY_LICENSES + README + site footer; README documents the lane.
- [x] T1.8 README dist section + canonical install copy; disclaimers (not affiliated with TypeSafe; Jev named only to compare).
- [x] T2.1 `gist-rs/reflex` created PUBLIC + seeded: `install.sh`, `install.ps1`, capabilities-only README, the no-source CI guard (zero `.rs`; push+dispatch only — no schedule, Actions budget).
- [x] T2.2 `gist-rs/homebrew-tap` Formula/riir-reflex.rb (manual commits: `9ac5696` → `5e3a153` → `8f8e28b`).
- [x] T2.3 `gist-rs/scoop-bucket` manifest — LANDED with the Windows asset (the no-404-manifest rule held): `bucket/riir-reflex.json` 0.1.1 → `riir-reflex-v0.1.1-x86_64-pc-windows-gnu.zip`, hash download-back-verified (`4414d72f…`). The three cross targets built LOCALLY via `cargo zigbuild` (windows-gnu + musl ×2 — `build-release.sh` routes non-host triples through it; no Actions minutes spent, the manual-deploy posture). Cross smoke: linux musl ×2 under docker (alpine, amd64+arm64) — `--version` stamp complete (no STALE), live serve + healthz + decide; windows exe smoke on the 4090 (same); leak scan PASS ×3; linux↔mac decide responses byte-identical. (`e9319bb`.)
- [x] T2.4 manual release on gist-rs/reflex: **v0.1.1** (aarch64-apple-darwin + x86_64-apple-darwin built locally, root-layout packages, SHA256SUMS verified, notes LINK the bench page). v0.1.0 was created and PULLED — its archives nested the binary under a version directory (the installer + Homebrew expect the root layout; found by T2.5's smoke) and the same-name `--clobber` re-upload did NOT survive the CDN cache (curl returned the old hash) — the cargo-heal no-latest-alias lesson, now measured from the publishing side.
- [x] T2.5 G1 install smoke — `curl | sh` clean-prefix install on this box, arm64 native + x86_64 via Rosetta, installer-hash-verified; `--version` stamp correct; live serve e2e (`/healthz` + `/decide` + CORS-closed default). FOUND the v0.1.0 archive-layout defect (above) — the smoke paid for itself.
- [x] T2.6 brew/scoop — brew upgraded from structural to MEASURED-FETCH: `brew tap gist-rs/tap <local clone>` + `brew fetch riir-reflex` downloaded the release through the formula and its sha256 verified against the release SUMS (`dac343eb…`); untapped clean. Scoop manifest landed with T2.3; the bucket's install row joins the parity gate as the 4th canonical command (T3.4 re-run PASS ×4).
- [x] T3.1 site repo created PUBLIC ([gist-rs/reflex-site](https://github.com/gist-rs/reflex-site)): `index.html` (dark-red rust theme `#B7410E`/`#7C1D05`), playground (fetches `http://127.0.0.1:7331/decide`; health probe + status pill + the exact launch command when the engine is absent), `/bench/` tables page, honest cards + disclaimers. Zero bindings, zero secrets.
- [x] T3.2 arena tables GENERATED from the harness output — `scripts/publish_bench.py` copies riir-reflex's `results.json` → `data/bench.json` SANITIZED (drops `datasets_dir` — the raw file carries `/Users/katopz/...`, which never reaches the site; grep-verified 0 hits). The bench page renders the json client-side; provenance (sha/date/host/protocols) rides inside the file.
- [x] T3.3 deploy — **reflex.gist.rs LIVE**: the owner attached the custom domain via the dashboard 2026-09-22 (200 verified on `/` + `/bench/`; the workers.dev URL still answers). Deliberately NOT declared in the site's `wrangler.toml` (which records why): a config `routes` block would make every future `wrangler deploy` require zone Workers-Routes + DNS permission neither .env token has (measured: deploy token = "No access to the specified resource" on `/zones/.../workers/routes`); a dashboard-attached domain persists across config-less deploys on its own.
- [x] T3.4 install copy parity — `scripts/install_copy_parity.py` (riir-reflex) asserts the 4 canonical commands (curl | sh · iwr -useb | iex · brew · scoop) appear identically in the dist README and the site page. PASS ×4.
- [x] T4.1 records: this plan + Proposal 014 status + riir-reflex AGENTS.md Phase-2 block (`9ff419b`) + all repos pushed.
