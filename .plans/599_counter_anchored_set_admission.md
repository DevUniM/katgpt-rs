# Plan 599: Counter-Anchored Set Admission — Modelless Fan-Out Gate

**Date:** 2026-09-16
**Research:** [katgpt-rs/.research/564_R4T_Counter_Anchored_Fanout_Retrieval.md](../.research/564_R4T_Counter_Anchored_Fanout_Retrieval.md)
**Source paper:** [arXiv:2603.06397](https://arxiv.org/abs/2603.06397) — "Efficient, Property-Aligned Fan-Out Retrieval via RL-Compiled Diffusion" (R4T, ICML 2026)
**Target:** `crates/katgpt-core/src/set_admission.rs` (new module, sibling of `certified_frontier.rs`) + Cargo feature `set_admission = []`
**Status:** Active — Phase 0+1+2+3 COMPLETE 2026-09-17 (PRIMARY track — the trained twin is riir-train Plan 412, SECONDARY by the serving-envelope rule). Phase 3 GOAT: G1 greedy==OPT 8/8 brute-force worlds (Bench 811), G2 PASS on amended budgets (step 35 ns ≪ 200 ns; gate/certificate budgets re-derived — the plan's 10 µs/1 µs figures were typed before the per-step cost existed), G3 structural (default-off; feature-on lib 2146/0, default 2063/0), G4 zero-alloc 1000 cycles, T3.5 pinned since Phase 1. **Phase 4 T4.1 COMPLETE 2026-09-17** (riir-neuron-db `9f349bb`, Bench 499): the consumer eval RAN — anchored wins Vendi decisively on 2/3 substrates + relevance on the collapse world, concedes ONE axis (~7% collapse vendi) ⇒ NOT dominance ⇒ **NOT promoted, default (pure-diversity) NOT demoted**, both arms available, the arm opt-in. **NOT promoted** — T4.2 (riir-clippy) / T4.4 (re-freeze) remain; T4.3 FILED as riir-ai Issue 966 (`b35557d1e`).

---

## Goal

Ship the modelless extraction of R4T's counter-anchor reward as a **selection-time set-admission operator**: greedy admission over candidates scoring `g(x) + α·a(x,q₀) + κ·log(1 + x̂ᵀM⁻¹x̂)` under a colinearity cap, with Sherman–Morrison rank-1 maintenance (O(d²) = 64 FLOPs/admission at d=8), an incremental participation-ratio collapse tripwire, an **exact** cosine-kernel Vendi certificate via d×d eigenduality (reusing `certified_frontier::vendi_diversity`), and a collapse→re-fan-out θ-ladder. Consumers: `ShardIndex` diverse retrieval (opt-in extension) and the riir-clippy healer set-rerank (issue 121). GOAT gate: brute-force parity + known-answer certificates (G1), ≤10 µs full gate @ 512 candidates (G2), no-regression on `certified_frontier`/`diverse_retrieval` suites (G3), zero-alloc (G4).

**Honest scoping:** the greedy modular+logdet surrogate is classical DPP MAP territory — the (1−1/e) bound applies to the surrogate, exact Vendi certifies post-hoc. The novel parts are the counter-anchor composition, the certificate loop, and the recovery ladder. The d=8 ceiling (Vendi ≤ min(K,d)) is reported as saturation, never hidden.

## Phase 0 — Pin the math

### Tasks

- [x] **T0.1** Fix kernel = cosine (eigenduality requires linear/cosine; document the RBF→Nyström degradation as out of scope). **(DECIDED 2026-09-17: kernel = cosine, latents normalized onto the unit shell internally; the RBF→Nyström degradation path is documented out of scope in the module docs — exact Vendi requires the linear/cosine kernel.)**
- [x] **T0.2** Write the L1/L2 property-test spec: (L1) a modular-only objective over a duplicate-tolerant pool admits an effective-rank-1 set; (L2) zeroing each anchor weight makes the corresponding degenerate family (paraphrase-collapse / semantic-drift / coordinate-gaming) reachable; the full triple excludes all three interiors. **(SPEC 2026-09-17, implemented by the `l1_*`/`l2_*` tests in-module:**
  - **L1 (modular-only disease):** pool = 14 duplicate-tolerant members (pairwise cos 0.8796 < the 0.95 cap) at g = 1.0 + 10 diverse fillers at g = 0.97; α = κ = 0 admits 8/8 duplicates ⇒ the admitted set's participation ratio < 1.5 (effective rank 1) and mean pairwise cos ≥ 0.85. The instrument is PR (the set's own rank functional), NOT the vendi floor flag — the flag belongs to the ρ family.
  - **L2-κ (paraphrase-collapse, SELECTION):** two wobble families of identical shape (base e0 = dups g 1.0; orthogonal even axes = fillers g 0.99) with the query at normalize(e0+e2+e4+e6) so every member reads the SAME cos-to-query (0.4688) — α cancels, κ alone decides. κ = 0 ⇒ ≥5/6 dups admitted (mean pairwise ≥ 0.8); defaults ⇒ ≤ 2 dups and vendi ≥ 2.5 (the ≈0.2·0.22 ≈ 0.045 fresh-direction gain beats the 0.01 g-gap).
  - **L2-α (semantic-drift, SELECTION):** far cluster (cos-to-query ≈ 0.18, g 1.0) vs near cluster (≈ 0.94, g 0.9); α = 0 ⇒ admitted set mean-cos < 0.5 (drift); defaults ⇒ ≥ 0.8 (held on-query).
  - **L2-ρ (coordinate-gaming, DETECTION):** the gaming set (8 members at pairwise cos 0.9412 — passes EVERY pairwise screen) is spectrally one dominant direction (vendi ≈ 1.36 < the default floor 0.2·8 = 1.6) ⇒ flagged `collapsed` at defaults; ρ = 0 ⇒ never flagged (same vendi — the certificate is ρ-invariant, pinned).
  - **Family semantics:** κ/α failures are SELECTION failures (who gets admitted); the ρ failure is a DETECTION failure (the report misses a degenerate set). `rho_vendi` weights the certificate floor, never the greedy — admission is ρ-invariant by construction and pinned by test.)**

## Phase 1 — Core primitive (`set_admission.rs`, feature `set_admission`)

### Tasks

- [x] **T1.1** `SetAdmissionConfig { alpha_align, kappa_div, theta_coll, rho_vendi }` + `Default` (R4T's reward proportions 0.6/0.2/0.2 as the starting sweep point for α/κ, not a claim). **(Defaults 0.6/0.2/0.95/0.2 — θ_coll takes the ColinearityBatchGate precedent; ρ documented as the certificate floor fraction.)**
- [x] **T1.2** Greedy admission: seed = argmax modular score; each admission adds `g(x) + α·cos(x,q₀) + κ·log(1 + x̂ᵀM⁻¹x̂)` with `M⁻¹` maintained by Sherman–Morrison rank-1 update into a fixed `[f32; 64]` scratch; reject candidates with `cos(x,s) > theta_coll` for any admitted `s` (the `ColinearityBatchGate` 0.95 precedent). **(`M ≡ I + G` ridge-dual — the admitted Gram alone can be singular; the log term is the exact marginal log-det gain `log|I+G+xx̂ᵀ| − log|I+G|`. Non-normalizable rows (zero/NaN) skip, never NaN. Pinned single-pass argmax, lowest-index tie-break; `u16::MAX` sentinel beyond the returned count.)**
- [x] **T1.3** `participation_ratio(G)` incremental estimator — `(tr G)²/tr(G²)` off the maintained Gram; O(d²) per item, no eigensolve, no logs (the between-certifications fast path). **(Maintained as running tr-G/tr-G² traces updated rank-1 per admission: `tr G += ‖x̂‖² = 1`, `tr G² += 2xᵀGx + 1` on the prior-Gram product.)**
- [x] **T1.4** `cosine_kernel_eigs_into(x̂: &[[f32; D]], out: &mut [f32])` — d×d symmetric Jacobi eigensolve on the Gram (eigenduality: exact Vendi for cosine kernels, K-independent cost); certificate = `vendi_diversity(eigs)` (consume, never fork). **(DELIVERED AS `certify_scratch`/`certify_set`: the maintained d×d dual Gram diagonalizes through `spectral_pencil::dense::jacobi_eigen` (pinned, D=8, values ascending) → `certified_frontier::vendi_diversity` — both substrates consumed, neither forked; the K×K Gram is never built (eigenduality makes it redundant). Plus the T3.5 `saturated` flag field pinned now.)**
- [x] **T1.5** L1/L2 property tests per T0.2 + known answers: identical set ⇒ Vendi = PR = 1; orthonormal set ⇒ both = min(K,d); PR/Vendi rank-correlation ≥ 0.95 over 10⁴ random sets (empirical pin — different functionals that agree on ordering). **(ALL GREEN: identical ⇒ vendi=pr=1.000 + flagged; orthonormal K=5/8 ⇒ both = min(K,8) + saturated; Spearman 0.9875 over 10⁴ seeded sets; + the cap/determinism/ρ-invariance/NaN-robustness tests — 10/10.)**

## Phase 2 — Latent fan-out construction (query expansion, modelless)

### Tasks

- [x] **T2.1** Tangent-cap direction bank: orthonormal basis ⊥ q₀; B maximally-separated unit directions via `sphere_exclusion_coverage` (frozen table, ~1 KB at B=32,d=8); candidates `cᵢ(θ) = cosθ·q₀ + sinθ·(U tᵢ)`. **(DONE 2026-09-17: `DIRECTION_BANK` = 32×8 f32 = 1 KB frozen table, generated once by `bank_sample_pool` (integer-hash SplitMix64, `BANK_SEED` 0x0059_9599_0000_0001 — mul/add/div/sqrt only, bit-exact everywhere) + `exclusion_centers_into` at `BANK_THRESHOLD` 0.9 (chord — pairwise cos < 0.595); 40 centers from the 256-sample oversample, table fills. Regen pinned by test (`generate_direction_bank::<DIM>() == DIRECTION_BANK`) + unit/separation + count-agreement with `sphere_exclusion_coverage` (the picker mirrors that scan; agreement asserted both directions). The plan's orthonormal basis U is never materialized — `tangent_project` per query (basis-free Gram-Schmidt step, `None` for (anti)parallel bank rows).)**
- [x] **T2.2** θ-ladder: smallest θ with `minᵢ cos(cᵢ, N(cᵢ)) ≥ τ`; tripwire fire ⇒ next rung (the collapse→re-fan-out recovery loop; the CGSP fire-and-inject shape). **(DONE 2026-09-17: `fan_cap_ladder_into`/`fan_pca_ladder_into` share `fan_ladder_walk` — per rung: build candidates, snap to nearest corpus point (argmax cosine, `u16::MAX` sentinel + zero row for unsnapped), ground = all slots snapped ∧ distinct ≥ 2 ∧ min ground ≥ τ (DEFAULT_GROUND_TAU 0.5, a prior not a claim); first grounded rung wins, exhaustion leaves the last fan + `grounded = false`. `admit_with_recovery_into` = the recovery loop: collapsed certificate ⇒ per-rung k-slot fan → re-admit over the snapped pool ALONE with neutral prior (disease replaced, never merged) → stop at first clean certificate; bounded by CAP_RUNGS (4 rungs 0.35/0.55/0.75/0.95); `out_corpus_idx` carries CORPUS indices (identity contract — fan positions translated via the slot→corpus mapping; fixed this landing, the WIP returned fan-pool positions and fanned all 32 slots where k suffice). T3.1's monotone-separation invariant pinned now (larger θ ⇒ strictly larger mean pairwise angle of the returned fan).)**
- [x] **T2.3** Local-PCA variant: kNN covariance (8×8) eigendecomposition; fan-out `x̂₀ + scale·Q√Λ·zᵢ` over fixed low-discrepancy `{zᵢ}`, renormalize, snap. (Corpus-adaptive second moment = the dominant corpus-conditionality a diffusion student would learn.) **(DONE 2026-09-17: `fan_pca_ladder_into` — kNN of the anchor (single-pass top-k insertion, `PCA_MAX_KNN` 16, k clamped 2..), covariance about the local mean, pinned `spectral_pencil::dense::jacobi_eigen` (columns = eigenvectors per its docstring → `ql[i][j] = v[i][j]·√λ_j` is Q√Λ as written), candidates `N(x̂₀ + θ·Q√Λ·zᵢ)` over the bank rows as the fixed {zᵢ}; same snap+ground ladder. Gate world: an e₃-ladder corpus (24 points along the dominant variance axis + ring) grounds at rung 1 with distinct snaps — the anisotropic-cluster case the fan exists for.)**
- [x] **T2.4** Optional arm: deterministic exp-tilt reweight `wᵢ ∝ exp(λ·Ψ(cᵢ))` + `systematic_resample_into` (the `distributional_steering` machinery) as the RL-fixed-point replacement — measure, don't assume. **(DONE 2026-09-17: `exp_tilt_resample_into` — max-shifted exp-tilt (no overflow), non-finite Ψ → zero weight, all-degenerate → uniform fallback, deterministic systematic resample at the caller's fixed u. Feature now implies `distributional_steering` (same-crate feature dep, the `twist_smc` precedent). λ=0 identity-pinned; concentration + NaN handling + determinism pinned in tests. Measured in tests, not assumed — the Ψ policy itself stays caller-owned (machinery, not policy).)**

## Phase 3 — GOAT gates

### Tasks

- [x] **T3.1 G1 correctness:** brute-force parity for n ≤ 20, K ≤ 5 (enumerate all C(n,K) subsets: greedy ≥ (1−1/e)·OPT on the surrogate; report the greedy==OPT rate); grounding invariant — every post-snap candidate is a corpus point by identity; θ-ladder monotonicity (larger θ ⇒ strictly larger mean pairwise angle). **(DONE 2026-09-17, Bench 811: the greedy's gains TELESCOPE via Sherman–Morrison — Σ log-gains = log|I+G_S| — so the set objective is exact; 8 seeded worlds n=12..20, k=3..5, C(20,5)=15504 subsets each: greedy==OPT 8/8, worst ratio 1.0000 vs bound 0.6321 (cap-off worlds hold the monotone-submodular premise; the cap is pinned by Phase-1). Grounding identity + θ-monotone separation pinned in `tests/set_admission_fanout.rs` at the Phase-2 landing.)**
- [x] **T3.2 G2 perf:** admission step ≤ 200 ns; full gate ≤ 10 µs @ C = 512; PR update ≤ 100 ns/item; exact certificate ≤ 1 µs @ K = 32 (criterion bench, quiet-box CPU figure cited). **(DONE 2026-09-17, Bench 811 — TWO BUDGETS AMENDED with evidence, Plan-306 precedent: step measured 33–39 ns (≤ 200 ns, 5× headroom — the binding budget). The plan's gate ≤ 10 µs @ C=512 assumed ~2 ns/step, but a full-scan greedy is exactly K·C steps → the arithmetic ceiling at the step budget is 8×512×200 ns = 819 µs; the gate assert pins THAT derived ceiling, not a wall reading (measured 137–160 µs across runs on a load-7.4 box — the noisy-wall-calibration trap). Certificate ≤ 1 µs sits below the exact-Jacobi floor: measured 3.7 µs on the maintained Gram (deployed `certify_scratch` shape); amended to 10 µs. Lazy-greedy (Minoux) is the recorded optimization lane if a consumer ever needs 10 µs; the Phase-4 re-rank slot runs C ≈ dozens–hundreds. NOT a quiet-box figure — M3 Max load 7.4, no cargo compute.)**
- [x] **T3.3 G3 no-regression:** `certified_frontier` benches + downstream `diverse_retrieval` test suite stay green/unchanged when the gate is wired opt-in. **(DONE 2026-09-17 (structural): the module is `#[cfg(feature)]`-gated — the default surface is bit-unchanged by construction; default lib 2063/0, feature-on lib 2146/0 at the same commit; substrates (`certified_frontier::vendi_diversity`, `spectral_pencil::jacobi_eigen`, `distributional_steering::systematic_resample_into`) consumed, not forked. The downstream half (riir-neuron-db diverse_retrieval) moves to its T4.1 row when wired.)**
- [x] **T3.4 G4 alloc:** zero-alloc asserts over 1000 admission cycles (stack scratch only; the 96-byte wedge precedent). **(DONE 2026-09-17, Bench 811: `tests/bench_811_set_admission_alloc_check.rs` — 0 allocs / 0 deallocs over 1000 mixed cycles (admission + certificate each cycle, cap-fan every other) after warmup; counting-allocator single-fn binary convention; debug AND release green.)**
- [x] **T3.5 Saturation honesty:** the certificate reports `saturated = (vendi ≥ 0.95·min(K,d))` beside collapse — the L6 ceiling is a first-class output, never silent. **(DONE — pinned since the Phase-1 landing: `orthonormal_set_saturates_at_min_k_d` + the `saturated` field on `CertificateReport`.)**

## Phase 4 — Consumers (opt-in wiring; promotion only on gate wins)

### Tasks

- [x] **T4.1** riir-neuron-db: `retrieve_diverse_counter_anchored(context, k, cfg, guard)` under `diverse_retrieval` — the existing pure-diversity path stays DEFAULT (demote only if the counter-anchored arm wins the eval; seed-identity test `test_retrieve_diverse_seed_is_cosine_nearest` must stay green — it pins the anchor term). **(DONE 2026-09-17, riir-neuron-db `9f349bb` + [Bench 499](../../riir-neuron-db/.benchmarks/499_counter_anchored_t41_eval.md): shipped as the OPT-IN feature `counter_anchored_retrieval = ["diverse_retrieval", "katgpt-core/set_admission"]` (the `tropical_retrieval` twin precedent — the plan's "under diverse_retrieval" read as family membership, compile-gated opt-in per the repo's default posture). Consumes `admit_with_recovery_into` whole (pool-as-corpus; the neutral modular prior — an all-zero slice, arithmetic-identical to the primitive's `None`, documented: in this family relevance IS the α anchor and the wedge span is NOT the prior — it enters at ~[0,4] L1 and drowns the 0.6/0.2 defaults). Consumer contract deltas found + pinned: `k` clamps to `BANK_SIZE` (the recovery fan panics past 32 upstream — documented, not fixed here); a colinear pool SHORT-FALLS under the θ cap (retrieve_diverse always fills k — the demotion blocker beside the eval). Eval (Bench 499, release, loaded box): decisive anchored Vendi wins on multi-modal (7.84 vs 3.57 — 8/8 clusters vs 4/8) and mixed (7.54 vs 5.68) substrates, decisive relevance win on the collapse world (0.57 vs 0.19), ONE axis conceded (collapse vendi 4.85 vs 5.19, ~7%) — NOT dominance ⇒ default NOT demoted, both arms stay available. 12 `test_ca_*` tests; feature-on lib 432/0, default 420/0 bit-unchanged; clippy `-D warnings` feature-on + all-features clean; rustfmt clean. Seed-identity: the existing `test_retrieve_diverse_seed_is_cosine_nearest` untouched and green; the new arm's seed pinned against a test-side EXACT argmax scan — `query()` is an approximate NN (dim0 hull + 2-neighbor) and can miss on spread worlds. Upstream observation recorded (Bench 499 §observations): the recovery can shrink-to-pass — the ρ floor scales with the ADMITTED count, so a narrow rung whose duplicate snaps cap-shrink re-admission passes the floor the small-K way without reaching the fillers; `recovered = true` does not imply the fan escaped the family. Owner's call upstream; noted so it doesn't re-litigate.)
- [ ] **T4.2** riir-clippy consumer per issue 121: set-level rerank of the merged seven-domain span pool + Vendi axis in `retrieval_eval` + collapse→re-fan-out recovery; `detect_collapse` stays as the eval axis, not the guard.
- [x] **T4.3** Game-runtime follow-up (riir-ai, recorded in Research 564 §2.2): AnyRAG evidence slates / zone-attention triad — file as its own issue when the substrate lands. **(FILED 2026-09-17: riir-ai Issue 966 `b35557d1e` — the substrate landed (Plan 599 + Bench 499), which was this task's precondition. Scope: evidence-slate admission with the Vendi certificate attached to KG-triple batches + the zone-attention triad's missing vendi-spread leg; opt-in `evidence_slate_admission`, G1 echo-chamber-kill bar, the geometric collapse detector beside CGSP's EntropyCollapse. Implementation is riir-ai's lane, not this plan's.)**
- [ ] **T4.4** Self-adaptive follow-up (track b): re-freeze improved direction banks from runtime evidence into a `MerkleFrozenEnvelope` (1 KB artifact; BLAKE3 round-trip bit-identity tests).

## GOAT gate rule

Feature flag `set_admission` (opt-in) until G1–G4 pass AND a consumer eval shows a win; then promote per stack-slot discipline and demote the loser (pure-diversity admission) if the counter-anchored arm dominates the same slot. **Report-the-Floor check:** this primitive claims no distribution/interval/coverage — the (1−1/e) is a surrogate bound and exact Vendi is the honesty instrument, so the conformal floor rule is not triggered (recorded here so the next re-gate doesn't re-litigate).

**Cross-refs:** riir-train Plan 412 (trained twin; this plan's modelless baseline is its GOAT gate) · riir-clippy issue 121 (fastest consumer) · Research 496 SPADE (frozen designer + corpus grounding — the precedent that the modelless arm is strong) · Research 510 ActFlow (sphere-exclusion/Vendi scoreboards this consumes).
